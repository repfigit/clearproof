# Clearproof adoption pilot implementation plan

Created: 2026-09-05  
Baseline: `4e7523147b26b23878265fa6759ec202e1a0b71e`  
Status: M0 implemented and locally verified on draft PR #27; M1–M5 remain open until their acceptance gates pass
Canonical location: `docs/plans/2026-09-05-adoption-pilot-implementation.md`

Acceptance review: [open requirement-by-requirement audit](adoption-pilot-acceptance-audit.md).

**Goal**

Implement a reproducible Clearproof pilot for a stablecoin processor or custodian that already has compliance systems. Deliver trustworthy transfer evidence, policy-change simulation, read-only transfer investigations, historical verification and observation-mode onboarding. Keep the verifier and portable formats open, and prepare managed integration and operational workflows for a paid pilot.

The active implementation goal covers **M0–M5** below. Credential-wallet expansion, provider-driven re-screening and production launch have explicit follow-on gates. This scope delivers an independently testable local pilot; it does not claim paying customers, regulatory approval, live vendor interoperability or production assurance.

Research inputs: [initial assessment](</home/agent/obsidian/Research Reports/Clearproof/Clearproof - Use Cases Adoption and Monetization - 2026-09-05.md>) and [additional improvements](</home/agent/obsidian/Research Reports/Clearproof/Clearproof - Additional Improvements for Adoption and Revenue - 2026-09-05.md>). These contain the external evidence and its limits. Local code and newly run checks determine implementation status.

**Delivery rules and architectural decisions**

- Build one vertical workflow with the existing Python API, PostgreSQL, TS proof SDK and CLI. Keep policy/business logic in Python; the proof SDK remains a thin cryptographic adapter.
- Define distinct results for cryptographic validity, policy decision, counterparty exchange, execution/settlement and historical evidence. Unknown or missing evidence must never silently become an authorization.
- Preserve raw PII exclusively in the encrypted envelope architecture. Apply tenant authorization to records, event ingestion, exports, replay and decryption. Logs and policy reports contain minimized references and reason codes.
- Use exact chain/asset identities, integer base units or validated decimal strings, explicit fiat valuation metadata and deterministic normalization. Bind the actual transfer facts to its receipt and proof statement.
- Version credentials, proof statements, public-signal schemas, policy profiles, receipts and discovery documents. Explicitly reject unsupported combinations. Coordinate Python, Circom, contracts, TS types and fixtures when the proof ABI changes.
- Retain the existing proving system unless the authenticated-input design requires a documented change. Verify approved artifact digests and provenance; local development keys must remain development-only.
- Start integrations with read-only ingestion and local simulators. Customer data, vendor account access, external communications, deployments and production fund movement are separate operational steps; none is needed to complete the local pilot.
- Use small reviewable changes. A milestone closes only with working code, meaningful acceptance tests and documented limitations. Do not convert failing tests into skips or declare a simulation to be live interoperability.
- Reuse and correct the existing roadmap and verifier specification. The old `AIF-82-implementation-plan.md` contains an unrelated repository assumption; treat it as historical, not an implementation authority.

**Milestones and dependency order**

| Milestone | Result | Depends on | Completion gate |
| --- | --- | --- | --- |
| M0 | Safe storage parsing, isolated chain caches, interoperable and constrained discovery | Baseline inspection | Regression cases reproduce the old behavior and pass after fixes |
| M1 | Authenticated proof/receipt foundation with durable tenant state | M0 | A valid synthetic transfer completes the real local proof path; adversarial bindings fail |
| M2 | Versioned policy evaluation and change simulation | M1 schemas and verification context | Deterministic comparisons explain every changed or indeterminate outcome |
| M3 | Read-only event ingestion and transfer investigations | M1 durable state and receipt IDs | Duplicate/reordered events reconcile to the same correct transfer state |
| M4 | Independently verifiable historical evidence bundles | M1 trust/version model; M3 event evidence | Offline verification succeeds for complete evidence and reports missing evidence accurately |
| M5 | Observation onboarding, executable scenarios and paid-pilot preparation | M2–M4 | A clean setup runs the complete documented pilot scenario and exports its results |

M2 and M3 have separable implementation work after M1 interfaces stabilize. M4 can begin with synthetic signed records once the trust model is settled. Dependency order does not authorize additional agents by itself.

No calendar completion date is promised before the M0 baseline and M1 proof-version design are measured. Report completed acceptance gates and actual blockers rather than percentage estimates.

**M0 — fix the newly confirmed integration and security gaps**

Local work-item IDs below are references for this plan, not filed tracker issues.

| ID | Change | Existing locations | Acceptance |
| --- | --- | --- | --- |
| CP-001 | Replace `eval` proof deserialization with validated JSON; support a bounded legacy migration and actual database row shape | `src/storage/proofs.py`, `src/storage/database.py`, `src/storage/models.py` | Real DB round-trip works; expressions and malformed/oversized signals fail; migration preserves valid records without executing data |
| CP-002 | Isolate caches by reader/deployment and define freshness/invalidation semantics | `src/chain/reader.py` | Two readers with different roots/registries cannot share results; concurrent calls, expiry and false/zero values behave correctly |
| CP-003 | Align Python publishing and TS consuming of discovery; validate exact requested identity, key purpose, versions and allowed destinations | `src/api/routes/discovery.py`, `src/protocol/discovery.py`, `packages/proof/src/discovery.ts`, `specs/well-known-clearproof.md` | Python-produced fixtures pass TS; mismatched DIDs, loopback/link-local destinations, redirect/rebinding cases and malformed keys follow explicit policy |

For CP-003, enforce destination policy at connection/egress boundaries, not just URL parsing. Permit explicitly configured private enterprise endpoints. Separate unsupported, unavailable and invalid responses. Document legacy key-field migration and prevent an encryption downgrade on discovery failure.

**M1 — make the underlying statement and evidence trustworthy**

This is the largest dependency. Establish the accepted proof statement before building product features on it.

| ID | Change | Existing locations or proposed boundary | Acceptance |
| --- | --- | --- | --- |
| CP-004 | Specify a canonical transfer/evidence schema and verification context; normalize asset, amount, participants, tenant, deployment, policy and time | `src/protocol/`, `src/api/routes/proof.py`, `specs/`, `src/protocol/bridges/trp_bridge.py` | Stable cross-language commitments; same-symbol assets on different chains differ; unknown assets and ambiguous amounts fail |
| CP-005 | Complete durable tenant-scoped credentials, proofs, roots, events, revocations and idempotency; wire issuance and proving to the same store | `src/storage/`, `src/registry/`, `src/api/dependencies.py`, auth middleware | Restart preserves state; cross-tenant access and conflicting duplicate requests fail; concurrent consumption cannot spend the same authorization twice |
| CP-006 | Implement a versioned authenticated credential construction binding subject/holder authority, jurisdiction, issuer and authorized issuance | `src/registry/credential_registry.py`, `src/registry/issuer_registry.py`, `circuits/credential_validity.circom`, affected contracts | An unissued credential, substituted subject/jurisdiction, unauthorized issuer and wrong holder fail in the applicable verifier, including the real proof path |
| CP-007 | Align witness generation and current verification acceptance across API, CLI/SDK context handling and contracts; correct policy units and confidential signal handling | `src/prover/`, `src/protocol/compliance_proof.py`, `circuits/`, `packages/proof/src/`, `packages/contracts/` | Shared valid and adversarial fixtures agree across implementations; no threshold-invalid “compliant” reference fixture; no public SAR advisory leakage |
| CP-008 | Complete trusted recipient encryption and tamper-evident durable evidence; bind envelope metadata and separate verification from state-changing authorization consumption | `src/protocol/hybrid_payload.py`, `src/sar/`, `src/storage/audit.py`, API routes | Recipient decrypts only authorized envelopes; wrong recipient/key/AAD fails; rotation overlap works; failure never falls back silently; audit tampering is detected |
| CP-009 | Bind approved runtime artifacts, proof version and policy schema to exact manifests; add a read-only `doctor` command | `packages/cli/src/commands/`, artifact loader paths, release metadata | Wrong/mixed/development artifacts receive explicit outcomes; production configuration rejects unapproved artifacts; diagnostics reveal no secrets |

CP-006 starts with a short architecture decision recording the authenticated-input construction, trust authorities and migration. Evaluate issuance membership under an issuer-authorized root using the existing Merkle machinery; retain issuer authorization checks. Issuer membership alone is insufficient. If signature verification stays outside the circuit, identify the trusted attestor and authenticate its binding to the proved inputs; never label that signature as ZK-verified. Resolve the design with adversarial vectors before updating the circuit ABI.

CP-007 must cover trusted roots and freshness, proof/credential expiry, future timestamps, jurisdiction and amount units, chain/contract audience, participants and actual transfer binding, issuer/VASP status, revocation and replay. Keep “inspect this proof” separate from “consume this authorization,” so read-only verification cannot accidentally spend a nullifier.

Remove the advisory SAR signal from externally disclosed proof material through a coordinated proof-version change, or redesign its confidentiality before enabling that profile. Merely omitting the named JSON field is insufficient if public signals still reveal it.

Generate changed development artifacts locally through documented commands. Do not commit newly generated development proving keys as production artifacts. Update the signal reference, vector-generation scripts, affected verifier source and version routing coherently; preserve historical verification of known prior versions without allowing unsafe legacy authorization.

M1 exit evidence includes one unmocked local issue → prove → verify → encrypt/decrypt → persist/restart → export flow, plus negative proofs for the trust and binding cases. Business-policy tests remain separate from expensive real circuit tests.

**M2 — policy evaluation and change simulation**

**CP-010:** add a bounded Python policy evaluator with immutable policy versions, provenance, approved source references and explicit `ALLOW / REVIEW / DENY / INDETERMINATE` results. Keep legal applicability, information requirements and private amount-tier predicates distinguishable. A configured threshold must not imply that all other obligations disappear.

**CP-011:** expose a `policy diff` CLI/API that compares two versions against synthetic or tenant-authorized cases. Include changed outcome, responsible rule, missing inputs, unsupported ZK coverage and review-queue impact. Store reviewed expected outcomes and policy approval history.

Acceptance: deterministic results for identical snapshots; boundary cases at threshold minus/equal/plus the smallest unit; conflicting rules; stale/unavailable sources; unsupported predicates; field minimization; cross-language proof/policy parity for shared predicates. Replaying a case must cause no counterparty communication or authorization consumption.

Entry points: `src/prover/tier_mapping.py` and existing policy fixtures; proposed `src/policy/`, authenticated API routes, CLI commands and versioned schemas. Keep business logic out of `packages/proof`.

**M3 — transfer investigations and reconciliation**

**CP-012:** add an append-only tenant-scoped event model, canonical business-transfer IDs, authenticated ingestion, deduplication, a transactional processing boundary and deterministic projections. Preserve event time, ingestion time, source and evidence references separately.

**CP-013:** implement one documented custody-provider adapter, initially Fireblocks, with captured schema fixtures and a local signed-webhook simulator. Recheck current official provider documentation during implementation. Add one local bilateral Travel Rule scenario using the existing bridge interfaces; validate mandatory message semantics instead of treating extension-compatible JSON as accepted interoperability.

**CP-014:** expose an investigation timeline and ageing queue through API and CLI, with a readable report. Join independent compliance, exchange, proof, custody and chain states. Include reason, owner, next action and provider links. Build a dedicated console only after the API/report demonstrates the workflow.

Acceptance scenarios: duplicate and out-of-order events; invalid authentication; restart during processing; approval without submission; counterparty timeout; failed settlement; settled transfer with missing evidence; changed canonical block/finality; cross-tenant identifier collision. Reordered deliveries must converge to the same result, and read-only processing must never initiate funds movement.

A fixture-tested provider adapter is a local acceptance result. Actual account integration and live interoperability require separate evidence at the customer gate.

**M4 — historical evidence and offline verification**

**CP-015:** define a portable bundle with a manifest of exact policy, signal-schema and verifier versions; authenticated decisions; source/root/status snapshots; transfer and envelope commitments; and available trusted timing evidence. Implement export and offline `verify-history`.

Configure trusted issuers, attestors, artifacts and time sources independently of the bundle. Distinguish a self-asserted time from an authenticated observation. Record decision, source-observation, capture and verification times separately. Use maintained cryptographic verification components; do not invent timestamp cryptography.

Return separate cryptographic checks and a historical outcome of supported, contradicted or indeterminate with scoped reasons. Missing revocation history must not be inferred from current status; later trust compromise must remain representable. Historical inspection cannot authorize transfer replay.

Acceptance: an offline process with preconfigured trust reproduces a complete synthetic record; modifying a manifest, swapping a root/key/policy or removing necessary evidence changes the result correctly. Test expiry, rotated keys, insufficient timing evidence and unauthorized bundle access. Retain only customer-authorized encrypted evidence; do not publish customer records to public transparency logs.

**M5 — onboarding, release evidence and monetization preparation**

**CP-016:** productize observation mode with a durable distinction from enforcement. Add deterministic counterparties for accept/reject/request-information/timeout/key-rotation/unsupported-version cases and duplicate/reordered custody events. Produce coverage, disagreement and latency reports with data minimization.

**CP-017:** provide a clean-environment quickstart, local service setup, reproducible synthetic demo, migration/rollback instructions, version compatibility matrix, observability and operator runbooks. Add an end-to-end acceptance command to CI. Correct stale claims and broken package/documentation references discovered during implementation.

**CP-018:** prepare a paid-pilot brief, customer interview script, baseline-measurement worksheet and pricing experiment. Specify a free verifier/portable format/local simulator and proposed paid managed connectors, policy approval history, reconciliation and evidence preservation. Include operational usage counters with defined retry/deduplication semantics, without requiring a payment processor integration.

Measure onboarding time, supported-case coverage, unexplained decision disagreement, time to diagnose an exception, stale-input exposure, evidence completeness and repeated operator use. These measurements support pricing discovery; generated reports and billing code do not establish willingness to pay.

M5 exit: a fresh checkout completes the documented synthetic scenario, exercises observation and all four decision classes, produces a policy comparison and investigation report, exports evidence, and verifies it offline. All outputs identify simulated counterparties and development assurance accurately.

**Follow-on work with explicit entry conditions**

| Track | Work | Start condition | Real completion evidence |
| --- | --- | --- | --- |
| F1: external credentials | One issuer/profile adapter; OpenID4VP verifier; approved issuer/claim/status policy; privacy-aware status cache | A prospective buyer identifies the actual issuer/wallet ecosystem and provides permitted test access | Applicable conformance tests plus two independently developed wallets; holder-to-transaction authority and status failure cases verified |
| F2: re-screening | Dependency index and targeted re-evaluation queue; preserve original decisions and deduplicate changes | A customer uses recurring decisions and an upstream provider supplies a permitted change feed or refresh API | Material upstream changes identify affected pending transfers; freshness and alert-quality measurements |
| F3: trusted distribution | Managed signed runtime/policy updates, rollback protection and controlled rotation | Customers need managed upgrades beyond pinned packages | Maintained update framework validation, key-rotation and rollback/freeze tests |
| F4: live paid pilot | Customer-authorized provider integration and observation deployment | Named budget owner, agreed scope/data permissions and account access | Repeated customer use, actual operational measurements and an agreed commercial arrangement |
| F5: production authorization | Independent audit, closed findings, documented trusted setup/approved artifact path and deployment operations | Pilot value demonstrated and appropriate assurance/resources available | Independent assurance evidence and explicit production readiness review |

F1–F5 remain visible follow-on work. Completing M0–M5 must not mark these complete or turn simulated evidence into claims of adoption.

**Verification and definition of done**

Use the current package scripts rather than stale command descriptions. At this checkout, root `npm test` runs Python; `npm run test:ts` runs the TS workspace tests.

For each change, run focused tests for the affected behavior, relevant lint/type checks and necessary integration tests. At each integrated milestone, run the appropriate wider suite. Before closing M5, require:

- `make test` for Python, including real PostgreSQL restart/tenant tests where relevant.
- `npm run test:ts` for workspaces and contracts; run contract checks explicitly if workspace orchestration does not cover them.
- `npm run build` and the TS `--noEmit` checks used by CI.
- Ruff checks/format validation for changed Python files.
- Dedicated unmocked circuit/proof tests for new proof statements and adversarial witness cases, separate from mocked compliance tests.
- The clean-setup end-to-end pilot, offline evidence validation, negative trust cases, authenticated webhook scenarios, migration/recovery checks and secret/PII-output assertions.
- Documentation/spec/vector alignment, actual artifact digest verification and a final repository diff review.

Record exact commands, outcomes and environment limits in the execution log below. Investigate baseline failures and distinguish unrelated failures from new regressions; do not claim the full gate passed when a required service or artifact was unavailable.

The active goal is complete only when M0–M5 pass these acceptance gates and the pilot documentation identifies every remaining external dependency. No revenue, production safety, audit completion or live provider support may be inferred from that result.

**Execution log**

| Date | Milestone | Evidence | Status |
| --- | --- | --- | --- |
| 2026-09-05 | Planning | Research converted into sequenced work, dependencies and acceptance gates; repository instructions and actual test scripts inspected | Complete |
| 2026-09-05 | M0 / CP-001 | Reproduced JSONB write, concurrent-startup and cleanup failures on PostgreSQL 18.6. Implemented bounded JSON signal validation, strict legacy-list conversion, database constraints, real row decoding, atomic serialized migrations, failure cleanup and explicit nullifier-conflict handling. All unit tests plus the real-DB proof-storage suite passed; a separate cancellation/retry test also passes. Ruff check passes. Migration/rollback guidance: `docs/operations/proof-storage-upgrade.md`. | CP-001 complete; M0 still open |
| 2026-09-05 | CP-017 | User prioritized website accuracy: authenticated GitHub API confirms main repository private; npm packages 0.3.0 versus checkout 0.4.0; standalone SDK install passes; public CLI install fails because `@clearproof/content` is unavailable; five Sepolia addresses have bytecode. Homepage deployed as clearproof-web commit `fd16d29`. Documentation refresh deployed to `docs.clearproof.world` (deployment `dpl_7FThp68fYo6NZifTPa2eBP7qUjNY`, code `c805646`). Production builds, homepage lint, shared content tests, mobile browser checks and live checks for 16 docs pages plus 3 content endpoints pass. The docs content API packaging fix is preserved in draft PR #26. Main content update is committed as `9452788`. | Partial; packaging and clean pilot remain open |

| 2026-09-05 | Verification baseline | CI run `33999156760` at docs commit `9452788`: TypeScript, license and protobuf checks pass. Python collection fails in the SIWE/ABNF dependency with a core-rule `ALPHA` error; Hardhat reports 36 passing, 2 pending and 19 failing (outdated constructor fixtures and sender-binding expectations); circuit setup download returns HTTP 403. Circuit lint also passes; final result is four checks passing and three failing. No Python, circuit or contract source was changed by the website refresh. Investigate these gates as part of M0/M1 and CP-017; do not claim full CI passes. | Open |

| 2026-09-05 | M0 / CP-002 | Reproduced cross-reader root leakage and swallowed RPC errors. Added deployment-local bounded caches, monotonic TTL, concurrent-call sharing, invalidation generations, defensive copies and strict 32-byte identifiers. Corrected bundled ABIs and proof-record decoding against current compiled contracts. Web3 wire-encoding/decoding fixture passes without live RPCs. All unit tests plus PostgreSQL proof-storage tests now pass: 231 tests; Ruff check/format and Hardhat compile pass. Added a dedicated PostgreSQL CI job for CP-001. | CP-002 complete; CP-003 open |

| 2026-09-05 | M0 / CP-003 | Replaced permissive discovery with the explicit 0.4.0 profile: full DID matching, usable canonical X25519 keys, fingerprint/purpose/suite/version checks and same-authority endpoints. Python/Node use connection-pinned DNS policy, verified TLS, bounded uncompressed responses, no proxies or redirects, exact enterprise CIDR exceptions and isolated bounded caches. API rejects discovery errors before proving/encryption; legacy v1 requires operator selection. Shared Python-produced metadata/adversarial/network fixtures and real local HTTPS tests pass. Final focused Python gate: 143 passed; SDK: 124 passed; SDK build, CLI typecheck and full `npm run build` pass (all four workspaces, Node 24.20). Unit + PostgreSQL storage + TLS integration run: 333 passed (before seven additional key/number tests). Ruff and diff checks pass. Migration/limitations documented in `specs/well-known-clearproof.md`. | CP-003 complete; M0 implemented on draft PR #27; M1–M5 open |
| 2026-09-05 | Verification baseline repair | Reproduced SIWE 4.4.0 import failure with ABNF 2.9.0 in an isolated environment. Pinned the tested ABNF 2.6.0; actual SIWE message parsing, EOA signing/verification, wrong signer/domain/nonce checks pass. Full local Python suite: 416 passed, 45 skipped (optional DB/artifact gates; real proof storage verified separately). Added dedicated discovery/TLS/API CI job. Prior PR #27 run `34000838114` confirmed the PostgreSQL job passes; contract fixture and circuit-download CI failures remain open. | Python collection repaired; remaining broader gates tracked |

| 2026-09-05 | CI portability correction | Run `34002411169` passed the PostgreSQL, TypeScript (124 tests), license, protobuf and circuit-lint jobs. Discovery/Python exposed an OpenSSL platform default reported as an unspecified TLS minimum. The owned context now sets an explicit TLS 1.2 floor; operator-context tests declare their floor and include unspecified-default and insufficient-floor regressions. All 16 real TLS tests pass locally. Contract fixtures and circuit download remain unchanged failures. | Verified in CI run `34002542623` at code commit `8b551b7`: discovery 145 passed; full Python 418 passed / 45 skipped; PostgreSQL, TypeScript, license, protobuf and circuit lint also pass. Only the existing 19 contract-test failures and circuit-download HTTP 403 remain. |

| 2026-09-05 | M1 / CP-004 foundation | Added strict private transfer, valuation, verifier-context and minimized receipt models; immutable exact-asset catalog; base-unit decimal parsing; bounded domain-separated canonical commitments in Python and TypeScript; generated schema exports and shared synthetic vectors. New Python tests: 45 passed; SDK total: 141 passed. Full monorepo build passes all four workspaces; Ruff and schema-export drift checks pass. Schema drift, same-symbol/different-chain identity, unknown assets, amount/valuation inconsistencies, context binding, privacy of repr/errors and serialization bounds are covered. `specs/transfer-evidence-v1.md` records the trust/activation boundary. | Partial: authenticated API/storage integration and circuit-facing transfer projection remain for CP-005–CP-007; M1 open |

| 2026-09-05 | M1 / CP-005 authorization foundation | Added a tenant principal dependency requiring authenticated tenant/actor claims, explicit roles and exact issuer scopes; development API keys require operator-selected scopes. Actual ES256 JWT tests reject forged signatures, missing claims and tenant header/query spoofing. Removed subject-wallet and freeform revocation-reason logging from the legacy credential route, with captured-log regressions. New principal tests: 12 passed. Full Python suite now passes 475 tests with 45 optional checks skipped; Ruff and format checks pass. | Partial: dependency is not wired to legacy routes; durable tenant state, SIWE membership and issuance/proving integration remain open |

Update this log with implementation evidence. Keep unchecked milestones open until their tests and deliverables are verified.
| 2026-09-05 | M1 / CP-005 encrypted storage foundation | Added additive migration 9 and authenticated tenant-bound encrypted records, immutable records with retained root revisions, explicit write/decrypt/consume roles, atomic actor-bound idempotency and tenant-scoped nullifier consumption. PostgreSQL 18.6 isolated-schema checks plus encryption tampering tests: 47 passed (32 existing proof-storage, 6 new pilot integration, 9 new cipher tests). Full Python: 484 passed, 51 optional checks skipped. Added pilot checks to PostgreSQL CI and documented key rotation, metadata visibility and rollback limits in `docs/operations/pilot-tenant-storage.md`. | Partial: legacy API issuance/proving are not wired; credential authenticity, service/process restart, real-proof integration and historical trust anchors remain open |
| 2026-09-05 | M1 / CP-006 authenticated construction | ADR 0003 defines two-level credential issuance/authorized-issuer membership, full tenant/issuer digest limbs, nonce, wallet, jurisdiction and holder-secret binding. Added the matching strict Python model and development Circom subcircuit; 29 focused checks passed, including 12 real compiled WASM witness checks for valid issuance, forged issuance/issuer, substituted subject/jurisdiction, wrong holder, tenant mismatch, timestamp boundaries, screening/tier and nonbinary paths. Added a standalone checksum-pinned Circom CI job independent of the existing ptau download. | Partial: registrar/root authentication, wallet-signed enrollment, durable issuance service, composed transfer ABI and real Groth16 proofs remain open; legacy profile unchanged |
| 2026-09-05 | M1 / CP-005–006 wallet enrollment integration | Added EIP-191 consent over the complete versioned credential and operator audience, with exact issuer-role/tenant checks, a ten-minute validity cap, canonical low-s EOA signatures and encrypted transactional persistence. `/pilot/credential/enroll` returns `awaiting-root-publication`; immutable nonce IDs and actor-bound retries survive app reconstruction/pool reconnection. Real JWT/EOA/PostgreSQL tests plus enrollment tests: 20 passed. Full Python: 526 passed, 52 optional checks skipped. Removed eager API package imports to resolve the auth/route import cycle. | Partial: enrollment is durable and authenticated, but trusted root publication, revocation, composed proving and OS-process restart acceptance remain open; legacy routes unchanged |
| 2026-09-05 | M1 / CP-006–008 registrar approval persistence | Added domain-separated Ed25519 root snapshots with tenant/deployment/issuer/key scope, tree/source evidence binding, revision predecessors and bounded validity. Operator-pinned trust verifies signatures; current verification additionally requires an independent expected head digest. Durable publication serializes competing successors, checks the signed predecessor and preserves history across pool reconnection. Root/storage focused tests: 25 passed. Full Python: 543 passed, 53 optional checks skipped. Enrollment parent 3a76730 passed eight CI jobs; the two existing Hardhat/ptau gates remain failed. | Partial: automatic trees from eligible enrollment, registrar policy, trusted head source, revocation, API/chain publication and composed proof integration remain open |
| 2026-09-05 | M1 / CP-005–006 durable revocation | Added authenticated `/pilot/credential/revoke`, exact issuer-scope checks before cached retries, immutable encrypted revocation records and preserved original timestamps. Added a transaction-scoped enrollment precondition checking tenant/ID/commitment, acceptance/expiry, screening and revocation. Real PostgreSQL/API tests cover wrong issuer, missing role, tenant collision, changed retries, scope removal, app/pool reconstruction and revocation rejection: 9 pilot integration checks passed. Full Python: 543 passed, 54 optional checks skipped. | Partial: root construction and composed proving must integrate the precondition; independently authenticated revocation snapshots and on-chain parity remain open |
| 2026-09-05 | M1 / CP-005–006 issuance tree construction | Added tenant-filtered keyset scans and deterministic sparse Poseidon trees with bounded capacity and dense-reference parity. Issuer-scoped construction rechecks retained wallet signatures, acceptance, tenant/deployment, commitment and revocation before including credentials; malformed evidence stops construction. Python-generated membership paths now drive real Circom witness checks. Combined enrollment/tree/circuit/storage checks: 61 passed; final PostgreSQL pilot checks: 10 passed, including revocation exclusion, tampered consent and scan overflow. Full Python: 552 passed, 55 optional checks skipped. | Partial: atomic construction/signing/publication coordination, trusted current heads and composed proving remain open; pilot scan explicitly capped at 256 tenant enrollments |
| 2026-09-05 | M1 / CP-005–008 atomic registrar refresh | Added `PilotRegistrar.refresh()` to build every operator-configured issuer tree, sign issuance and aggregate issuer approvals, persist encrypted source evidence and cache the receipt under one tenant transaction. Migration 10 adds immutable root-source records; direct and coordinated approvals share signature/predecessor checks. Real PostgreSQL tests confirm complete rollback when a later issuer lacks signing scope, conflicting refresh rejection, retry/reconnect stability, revoked-entry removal and an actual competing revocation waiting on the PostgreSQL advisory lock. Storage/migration tests: 43 passed. Full Python: 552 passed, 56 optional checks skipped. | Partial: independently trusted current-head publication, registrar API/chain integration, authenticated revocation evidence and composed real proofs remain open |
| 2026-09-05 | M1 / CP-008 independent checkpoint implementation | Added `PilotRootCheckpoint` with tenant-scoped publishers, compare-and-set monotonic approval revisions, scalar/time constraints and historical events. Python authenticates snapshots against a configured chain/address/runtime pin and a single block observation, checking current digest/root/revision/validity and re-reading the block hash; no transaction or consumption occurs during reads. Real local EVM: 4 focused Solidity tests and 1 Python integration flow passed, including stale approval, wrong context/bytecode and unchanged publisher nonce. Added an owned ephemeral-node runner and standalone CI job; generated TypeChain bindings via Hardhat. Full Python: 552 passed, 57 optional checks skipped. | Partial: no shared-network deployment; operator manifests/publisher delivery, production finality and trusted RPC operations, offline historical chain evidence and composed proof integration remain open |
| 2026-09-05 | M1 / CP-007 valuation arithmetic | Added unsigned 128×128→256 limb multiplication, exact quotient/remainder valuation and private tier predicates without SAR outputs. Python witness builders use integer arithmetic. Thirty focused tests passed, including actual WASM rejection of a crafted field-modulus alias, near-256-bit products, equation-preserving oversized remainder, zero/out-of-range inputs and threshold minus/equal/plus boundaries. Extended the real-circuit CI job. Full Python: 582 passed, 57 optional checks skipped. ADR 0004 documents the integer argument and remaining trust binding. | Partial: quantities, quote provenance and policy thresholds still need binding into the composed transfer commitment; legacy public signals unchanged |
| 2026-09-05 | Checkpoint CI portability repair | Checkpoint CI passed Solidity checks but its Python runner failed because clean npm installation hoisted Hardhat outside the workspace. Replaced the assumed filesystem path with Node module resolution. Verified a hoisted-only layout and reran the owned local-EVM integration successfully. | Local fix verified; new CI run pending |
| 2026-09-05 | M1 / CP-004–007 private transfer projection | Added a fixed 48-field Python/Circom projection binding canonical transfer/context digests to direct participant, asset, valuation, policy and timing operands. It uses full digest limbs, integer range checks and a verifier-recomputed Poseidon commitment. A separate tenant/transfer/nonce/deployment scope keeps holder nullifiers stable across re-evaluation. Twenty-two focused tests passed, including real WASM rejection of substitutions and invalid predicates under recomputed commitments. Full Python: 604 passed, 57 optional checks skipped. ADR 0005 and the signal reference define the exact layout and trust boundary. | Partial: parent credential/holder/sanctions composition, trusted private-record attestation and the final public proof profile remain open |
| 2026-09-05 | Checkpoint CI repair verified | CI run 34006940956 at c0e4e19 passed nine jobs, including the isolated checkpoint EVM job and credential/valuation witness checks. The two existing Hardhat and trusted-setup gates remain failed. | New projection commit awaits CI |
| 2026-09-05 | M1 / CP-006–007 composed development proof | Added separate `pilot-transfer-v1` with eight public inputs and no public outputs, joining holder-bound originator credential issuance/issuer membership, the private transfer projection, integer valuation, expiry/domain/nullifier checks and direct sanctions checks for both wallets. Raw-address domain-301 sanctions trees are explicitly limited to 254 local fixture entries and incompatible with legacy roots. Nineteen focused tests passed, including real WASM rejection of substituted holders/participants/roots/domains and unrelated sanctions gaps. A local Groth16 setup/prove/verify round trip succeeded; all eight public signals matched Python and a modified public commitment rejected the proof. Full Python: 623 passed, 57 skipped, 30 warnings. ADR 0006 records reproduction and trust boundaries; CI includes composed witness tests. | Partial: unapproved local development keys only; authenticated private-record/quote/policy attestation, current root/revocation integration, artifact manifests, API/SDK/contracts acceptance parity and the complete restart/export pilot remain open |
| 2026-09-05 | M1 / CP-009 pinned artifact inspection | Added a strict versioned development manifest binding the composed profile, exact public signal order, transfer/context schemas, declared provenance digests and four artifact byte hashes/sizes. The read-only Python doctor requires an independent operator pin, rejects production mode, mixed files, duplicate JSON keys, traversal, symlinks, FIFOs and legacy key layouts, and returns captured verified key bytes. Added context-to-manifest/profile matching. Actual local composed artifacts inspected successfully as `development_unapproved`; the local synthetic policy marker is explicitly not approved policy binding. Full Python before the final context helper: 642 passed, 57 skipped, 30 warnings; final focused artifact tests: 20 passed. | Partial: source/ceremony provenance is not established by file hashes; runtime pins, authenticated policy loading, package CLI and API/SDK/contract enforcement remain open |
| 2026-09-05 | M1 / CP-007–009 pinned read-only pairing | Added exact finite affine Groth16/BN128 proof parsing, canonical eight-scalar validation and trusted-caller expected-signal comparison before the pairing. The verifier uses captured inspected key bytes and a separately pinned self-contained snarkjs bundle, with no npx/download/package resolution or customer-data files. A minimal child environment, heap bound and timeout/cancellation cleanup isolate the owned runtime; results expose cryptographic validity only. Twenty-six focused tests passed, including a real development proof, altered proof/signal rejection and actual Node timeout/cancellation reaping. Full Python with real local pairing integration enabled: 669 passed, 57 skipped, 30 warnings. CI at preceding 62d8c5f passed nine jobs including composed witness checks; Hardhat and trusted-setup jobs remain failed. | Partial: Node/OS remain trusted host components; independently authenticated expected state, runtime/manifest integration, API/SDK/contracts acceptance parity and consumption remain open |
| 2026-09-05 | CI contract fixture repair | Fixed legacy registry fixtures to deploy and activate a real VerifierRouter and pass its selector to the current constructor. Domain tests now use the registered wallet and canonical packed-address hash with valid preceding context. Threshold tests require the exact pairing rejection instead of accepting any non-threshold failure. The invalid-proof test no longer swallows assertion failures, and the cross-chain case now uses committed vectors instead of optional `/tmp` files. Forty focused checks pass. Full Hardhat in an isolated copy of tracked files with installed dependencies reused: 60 passed, 1 artifact-dependent E2E skip. Existing untracked local legacy WASM exposes a different witness/credential layout and fails the full local E2E; it was not silently accepted or regenerated as production material. | Partial: CI verification pending; Powers of Tau download still returns HTTP 403, and a fresh matching legacy end-to-end artifact set remains needed for that optional test |
| 2026-09-05 | CI development circuit pipeline | Verified run 34008904073 at 6d4bf30: ten jobs passed, including the repaired full Hardhat suite; only the Powers of Tau HTTP 403 gate failed. Replaced that download-dependent development job with an explicit isolated local setup script, preserving legacy compile/key generation/CLI prove-and-verify and adding composed compile/key generation, artifact inspection and real adversarial pairing integration. Both unchanged circuits fit power 16 (legacy 30,164 total constraints; composed 50,992), so CI generates a power-16 development transcript. A complete local invocation generated its own phase-one contribution/preparation and both phase-two development key sets; the legacy CLI proof verified and the composed real integration passed. Generated Solidity, keys and vectors stayed outside tracked source; uploads are explicitly `UNAPPROVED`. Workflow YAML parses and script Ruff checks pass. | Partial: replacement CI run awaits verification; local/single-party development setup is not production ceremony approval, and authorization/parity/restart/export milestones remain open |
| 2026-09-05 | M1 / CP-006–007 scoped valuation approvals | Added purpose-separated Ed25519 approvals over the exact valuation, tenant, catalog digest, signing time and key ID. Operator trust scopes keys to exact assets/sources/catalogs and quote lifetime/observation age. The composed witness builder now requires an approved quote and checks it at the claimed evaluation time before projection. Real-signature tests reject consistent forged rates/evidence, cross-tenant/catalog/asset/source reuse, stale/future/expired quotes and cross-purpose signatures; key overlap/removal works. Thirty-nine focused quote/composed checks passed; full Python with the real local pairing integration enabled: 689 passed, 57 skipped, 30 warnings. ADR 0007 records authority assumptions and the required independent current-verifier check. | Partial: quote source truth remains a configured authority assumption; live ingestion, retained signed quote evidence, authenticated policy thresholds and current API/SDK/contract acceptance integration remain open. Circuit ABI unchanged; Python encoder checks cannot prevent a prover bypassing the encoder |
| 2026-09-05 | M1 / CP-007 and M2 policy-input foundation | Added immutable bounded `PilotPolicy` records with exact USD-cent thresholds, tenant/deployment/jurisdiction/catalog scope, effective intervals and source-review provenance. Independent current-digest pins select exactly one policy per scope, including across catalog versions; drafts cannot select themselves and predecessor links must extend the same policy identity/scope consecutively. The composed encoder now derives thresholds from that policy instead of accepting a free tuple. Forty-one focused policy/composed tests passed, covering boundary cents, stale/draft/scope substitution and fabricated histories. A fresh policy-bound development proof passed real pairing/tamper checks. Full Python with real pairing enabled: 711 passed, 57 skipped, 30 warnings. ADR 0008 separates private tiers from business/legal decisions. | Partial: independent current-verifier enforcement, durable approval/source history, manifest policy-schema binding, business rule evaluation and policy diff remain open; CI development circuit run 34009491967 remains active |
| 2026-09-06 | M2 / CP-010 bounded business-policy evaluator | Added up to 64 source-referenced rules to the immutable policy record and a pure evaluator producing ALLOW/REVIEW/DENY/INDETERMINATE. It validates tenant/transfer/context scope, effective intervals, unique bounded facts, exact cent comparisons and a minimum evidence-completeness profile; low amounts cannot bypass other required checks. Unsupported predicates remain explicit; inactive policies yield indeterminate and active-rule conflicts use documented precedence. Reports omit raw amounts/wallets/fact values, sort diagnostic IDs and do not claim ZK coverage. Sixty focused evaluator/policy/composed checks passed; full Python with real pairing enabled: 730 passed, 57 skipped, 30 warnings. | Partial: evaluator trusts caller-authenticated snapshots; durable fact/approval history, current authorization integration and policy-diff API/CLI remain open. Circuit CI run 34009491967 remains active |

| 2026-09-06 | M2 / CP-011 policy comparison API and Python CLI | Added bounded deterministic before/after comparison over 1–64 supplied cases, with decision/explanation changes, candidate changed rules, missing/unsupported inputs, explicit absent ZK coverage and review-count impact. Duplicate business transfers reject to prevent inflated counts. The stdin CLI avoids plaintext input files; the JWT-protected API requires policy:read plus evidence:decrypt, enforces the authenticated tenant, bounds uploads and returns generic errors without echoing input. Nine focused checks passed; full Python with real development pairing enabled: 739 passed, 57 skipped, 30 warnings. | Partial: durable reviewed expected outcomes and policy/source approval histories, packaged CLI integration and authenticated stored-case loading remain open. CI run 34009491967 is now terminal failure: development phase-2 Powers of Tau preparation exceeded its 1,800-second subprocess timeout; local proof evidence does not establish CI success. |

| 2026-09-06 | Development circuit CI timeout follow-up | Inspected the failed run's traceback: phase-two preparation exhausted its 1,800-second limit before compilation/proofs. Increased only that command's allowance to 5,400 seconds with verbose progress and a 150-minute job ceiling. Added elapsed command reporting and owned process-group cleanup. Two real subprocess tests pass, including timeout of a parent with a spawned worker; Ruff passes. | CI completion remains unproven; the longer allowance must be validated by the remote run. No proof checks were removed and no production artifacts were introduced. |

| 2026-09-06 | M2 / CP-011 durable reviewed policy service | Added encrypted immutable policy approvals and separately retained reviewed snapshots. Authenticated reviewers require policy:approve and evidence:decrypt; actor identity is taken from the principal. Bounded unique cases must reproduce their specified expected outcomes before approval. Retained predecessor identity/scope/revision/time is checked, and case records plus approval/idempotency result share the tenant transaction. Real PostgreSQL reconnect, ciphertext privacy, tenant isolation, predecessor, immutable conflict and bad-outcome checks pass. Focused policy/storage run: 62 passed. Full Python with development pairing enabled: 741 passed, 58 skipped, 30 warnings; the separate focused run explicitly enabled PostgreSQL. | Partial: approval HTTP route, authenticated stored-case loading, independent activation/history, retained source documents and offline review authenticity remain open. Approval records do not activate policies or establish source truth. |

| 2026-09-06 | M2 / CP-011 authenticated retained policy workflow | Added POST /pilot/policy/approve with verified reviewer identity, server clock, bounded redacted parsing and durable idempotency. Added POST /pilot/policy/diff/stored to load tenant-scoped encrypted approvals/cases, recompute content digests and produce deterministic read-only comparisons. Real signed-JWT/PostgreSQL tests cover approval, retry, reconnect, forged actor/time rejection, absent/insufficient roles, tenant isolation, repeated comparison, minimized output and unchanged record/consumption counts. Focused storage/API run: 22 passed. Full Python with development pairing enabled: 741 passed, 59 skipped, 30 warnings (PostgreSQL was explicitly enabled in the separate focused run). | Partial: source-document retention, activation history, packaged CLI command and independent offline review authenticity remain open. Initial local test attempt hit /tmp quota before API execution; rerun used isolated UTF-8 PostgreSQL and temporary files on the workspace filesystem. Circuit CI run 34011343899 remains confirmed in progress. |

| 2026-09-06 | M2 / CP-011 policy diff command | Added the unreleased TypeScript CLI policy diff command for supplied snapshots and --stored references, delegating evaluation to the authenticated API. Bearer tokens come from the environment; private JSON enters stdin. Explicit origin validation, redirect rejection, bounded input/response, deadlines and generic failures protect the transport boundary. Build and five focused transport/built-command checks pass; CI now builds CLI and runs these checks. Corrected CLI setup documentation to distinguish private source/unreleased commands from the broken public 0.3.0 install. | Partial: combined CLI-to-real-API pilot and public packaging remain unverified. Full CLI run: 7 passed, 1 existing demo-artifact failure because bundled WASM/key files are absent; no artifacts were fabricated to satisfy it. Source retention, activation history and offline review verification remain open. Circuit CI run 34011343899 remains in progress. |

| 2026-09-06 | M2 / CP-011 combined CLI/API integration | Added an opt-in integration gate running the built Node CLI through real loopback Uvicorn, verified JWT authorization and isolated UTF-8 PostgreSQL. Supplied-case and retained-case reports equal the direct API result after database reconnect; foreign-tenant access rejects without exposing digests, and record/consumption counts remain unchanged. The database CI job now builds content/proof/CLI and explicitly enables the gate; missing prerequisites fail when enabled. All 13 PostgreSQL integration checks passed with the gate enabled; Ruff and diff checks pass. | Policy workflow integration is locally verified; complete proof-pilot and public package release evidence remain open. Source-document retention, activation history and offline trust verification remain open. Circuit CI run 34011343899 is still in progress. |

| 2026-09-06 | M3 / CP-012 deterministic event model and projection | Added tenant/deployment business-transfer scope, immutable bounded event records with separate source sequence/event time/ingestion time/evidence reference, and six independent state dimensions. Replay deduplicates identical source events, rejects conflicting identities/sequences, selects per-source sequence heads and exposes source/block conflicts without arrival-order precedence. Timeline/findings cover approval without submission, counterparty timeout, failed settlement, reorganization and finalized observations with unresolved evidence, including reason/owner/next-action/age. Eight focused checks pass, including all permutations of a lifecycle and duplicate retries. Full Python with real development pairing enabled: 749 passed, 59 skipped, 30 warnings; Ruff/diff checks pass. | Partial: caller source authentication is explicit, and durable ingestion/transactional restart, provider adapter, provider links, complete queue rules and API/CLI reports remain open. No live provider or complete M3 acceptance claim. Circuit CI run 34011343899 remains in progress. |

| 2026-09-06 | M3 / CP-012 durable internal event ingestion | Added independently configured tenant/deployment/source/actor/dimension/time grants, server-stamped ingestion, immutable encrypted events and migration 11's tenant event index with identity/sequence uniqueness and foreign keys. Event and index writes share one tenant transaction; duplicate retries preserve receipt time and source conflicts roll back. Scoped investigation loads indexed events without scanning unrelated records. Fifty-four focused storage/migration/replay checks passed with real PostgreSQL and the policy CLI integration enabled, covering concurrent duplicate delivery, reconnect, late older events, post-insert sequence rollback, source authorization and cross-tenant collisions. Full Python with development pairing enabled: 749 passed, 60 skipped, 30 warnings; PostgreSQL was separately enabled for the focused run. | Partial: abrupt process-death acceptance, HTTP ingestion, provider webhook authentication, historical source grants, queue/report integration remain open. The pilot rejects over 256 unique events per transfer rather than truncating; scalable archival/pagination is not yet implemented. Circuit CI run 34011343899 remains in progress. |

| 2026-09-06 | M3 / CP-012 authenticated event API and crash rollback | Added bounded JWT-protected ingestion/investigation routes, server-stamped receipt time and operator-only source grants. Shared the private-body reader with policy routes while retaining their limits. Real JWT/PostgreSQL checks reject absent roles, ungranted sources, conflicting/oversized events and forged timestamps without echoing values; retained replay survives reconnect and disabled ingestion configuration. A separate ingester process is SIGKILLed after encrypted insertion but before indexing: PostgreSQL rolls back and retry creates exactly one complete event. Thirty-three focused checks passed with PostgreSQL and policy CLI integration; full Python with development pairing enabled: 749 passed, 62 skipped, 30 warnings. | Partial: provider authentication, historical source authority, queue/CLI reports and complete M3 scenario remain open. Abrupt rollback is verified at the tested transaction boundary, not every possible crash point. |
| 2026-09-06 | CI development proof verification | Run 34011343899 at commit 3953cff completed successfully: all 11 jobs passed. Circuit logs confirm fresh development round trips and pairing integration completed with artifacts explicitly unapproved. | Resolves that revision's setup-timeout verification gap. Newer policy/reconciliation commits still require their own CI results; full integrated pilot and production assurance are not established. |

| 2026-09-06 | M3 / CP-013 Fireblocks V2 verification profile | Rechecked official signature, V2 migration, transaction-field and ordering documentation. Implemented bounded detached RS512 verification through PyJWT/cryptography with an independently provisioned JWKS interval, explicit workspace/transaction/external-ID/asset binding, generic rejection and minimized custody observations. Added a synthetic documented-schema fixture and ephemeral RSA signed-notification simulator. Unsupported headers/events/statuses, stale/future input, wrong keys/scopes and body tampering reject. Notification timestamp ordering is explicit; equal-time conflicts are not resolved by arrival. COMPLETED maps only to custody completed, without asserting chain finality or compliance. Twenty-one focused checks passed; full Python with development pairing enabled: 762 passed, 62 skipped, 30 warnings. | Partial: this is the local verifier/normalizer, not a connected Fireblocks account. Durable raw-body/signature retention, provider intake, JWKS refresh/provenance, real fixture validation and bilateral Travel Rule scenario remain open. Sources and exact profile limitations are in docs/internal/PILOT_FIREBLOCKS_ADAPTER.md. |

| 2026-09-06 | M3 / CP-013 durable signed Fireblocks intake | Connected the pinned verifier to authorized internal event ingestion. Migration 12 permits encrypted provider-evidence chunks and manifest records. Exact body bytes (including multi-chunk Unicode), detached signature, selected public key/configuration and pinned binding commit atomically with the normalized event/index. Duplicate observations preserve original receipt/evidence; event-only records cannot silently stand in for provider-backed retries. PostgreSQL tests reconstruct exact bytes after reconnect, reverify with independent test trust, reject foreign-tenant reads and tampering, and prove evidence rollback when sequence insertion conflicts. Sixty-two focused storage/migration/provider checks passed with real PostgreSQL and policy CLI integration; full Python with development pairing enabled: 762 passed, 63 skipped, 30 warnings. | Partial: this is a local signed simulator-to-durable-store flow. Public/provider HTTP intake, authenticated JWKS refresh, historical authority receipts, live fixture validation and bilateral Travel Rule scenario remain open. No raw provider payloads or production keys were committed. |

| 2026-09-06 | M3 / CP-013 authenticated Fireblocks relay | Added POST /pilot/fireblocks/{integration_id}, requiring a tenant JWT, one detached provider signature, operator-pinned integration/key/transaction binding and a separately configured source grant. It preserves exact body bytes, bounds uploads, stamps server time and uses the atomic encrypted intake. Real JWT/PostgreSQL checks cover retry after reconnect, missing roles, foreign/unknown integration IDs, signed wrong-workspace payloads, byte tampering, duplicate/legacy signature headers, size limits and disabled grants/configuration; rejected inputs add no evidence or consumption. Thirty-one focused checks passed; full Python with development pairing enabled: 762 passed, 64 skipped, 30 warnings. | Partial: this is an authenticated internal relay, not a direct externally deployed provider listener. JWKS refresh/provenance, historical authority, live account validation and bilateral Travel Rule work remain open. CI run 34013000901 for the preceding commit was confirmed in progress; latest-head CI is not yet verified. |

| 2026-09-06 | M3 / CP-014 ageing queue API | Added tenant-scoped keyset scans and POST /pilot/events/queue with bounded pages, minimum finding age, scanned counts and explicit continuation. Queue entries minimize output to opaque scope, independent states and reason/owner/next-action/age findings. Loaded events now also verify their indexed scope and tenant. Real JWT/PostgreSQL tests cover deterministic age sorting within pages, complete quiescent traversal, empty filtered-page continuation, exact age boundaries, reconnect, tenant/role rejection and unchanged storage/consumption counts. Twenty-seven focused checks passed; full Python with development pairing enabled: 762 passed, 65 skipped, 30 warnings. | Partial: page ordering is explicitly scope-pages-age-within-page, not a global priority snapshot. CLI collection/rendering, provider links, upstream event coverage and broader queue rules remain open. Only indexed transfers appear; scans during concurrent new-transfer insertion require refresh. |

| 2026-09-06 | M3 / CP-014 investigation CLI reports | Added unreleased investigation timeline/queue commands with private stdin, environment token, readable output and JSON mode. Extracted the bounded authenticated HTTP client shared with policy comparison. Queue collection follows bounded pages through empty filtered results, rejects duplicate scopes/nonadvancing cursors, sorts collected ages and preserves continuation with explicit partial status and page times. A 60-second collection deadline and 1–32 page budget bound work; terminal controls reject during rendering. CLI build and all ten focused policy/investigation checks pass, including built-command subprocess execution. CI includes both suites. | Partial: combined investigation CLI-to-real-API evidence, provider links and complete workflow event coverage remain open. Collection is not a frozen global snapshot; public npm packaging remains unreleased and the known missing bundled-artifact test is not claimed resolved. |

| 2026-09-06 | M3 / CP-014 combined investigation CLI/API evidence | Extended the existing enabled-in-CI PostgreSQL gate to run built Node investigation commands through real loopback Uvicorn and signed JWTs. Timeline JSON matches direct service output after reconnect; collection of four one-transfer pages matches the full API queue, while a one-page budget retains explicit partial status/cursor. Readable findings, cross-tenant rejection, missing-role rejection and unchanged event/consumption counts pass. All 19 PostgreSQL integration checks passed with both CLI workflows enabled; Ruff/diff checks pass. | Local investigation CLI/API integration is verified. Provider links, full workflow event generation, bilateral scenario and complete proof-pilot remain open. CI run 34013588143 for the preceding CLI commit was confirmed in progress, not assumed passing. |

| 2026-09-06 | M3 / CP-014 adverse-state and changed-block findings | Added investigation findings for compliance denial/review, invalid proof, counterparty rejection/information requests, custody cancellation and incomplete evidence. Custody completion without an independent finality observation remains unresolved. A source changing its confirmed/finalized block identity produces a historical discrepancy finding, with stable age across repeated replacement-block observations and deterministic reordered delivery. Focused reconciliation/provider checks: 31 passed. Real PostgreSQL/API/CLI integration: 19 passed. Full Python with development pairing enabled: 772 passed, 65 skipped, 30 warnings. | Partial: discrepancy acknowledgment/resolution, provider links, full workflow event coverage and the bilateral scenario remain open. Findings prompt review; they do not independently verify chain assertions or authorize transfers. CI run 34013677002 for preceding commit 6094f67 remains in progress. |

| 2026-09-06 | M1 / CP-009 policy-schema artifact binding | Published the runtime policy JSON schema with a canonical domain-separated digest. New development manifests use that digest instead of an unbound zero marker. Artifact context checking rejects an unsupported policy schema even for a pinned manifest; read-only inspection explicitly reports compatibility for older bundles. Regression tests cover published/runtime schema drift, digest changes and pinned incompatible schemas. Sixty-seven focused artifact/verifier/composed checks passed; full Python with existing real development pairing enabled: 775 passed, 65 skipped, 30 warnings. Ruff/diff checks pass. | Partial: structural schema binding does not cover model-validator/evaluator semantics or approve a policy revision. Fresh artifact generation using the revised builder is not yet verified; current authorization integration and complete context/manifest/proof binding remain open. Existing pairing evidence proves cryptographic inspection, not those current-acceptance gates. |

| 2026-09-06 | M1 / CP-007–009 fresh manifest-bound development proof | Reordered development generation so the finalized artifact manifest is available before synthetic witness construction. The retained synthetic context now commits the actual manifest digest. The pairing gate reconstructs expected context/signals, rejects a substituted manifest and its projection, and checks real proof tampering. Artifact changes alter the projection while preserving the authorization nullifier. Fresh legacy/pilot compilation, development key generation and round trips passed using the previously prepared local development ptau; the new bundle reports supported policy schema and rejects production mode. Forty-three focused tests passed; full Python using this fresh bundle: 776 passed, 65 skipped, 30 warnings. | Local fresh generation now verifies the prior schema-builder gap and synthetic context/manifest/proof binding. This reused a development ptau, not a new trusted ceremony or clean environment. Durable current authorization, verifier parity, trusted encryption, complete historical bundles and the full M1–M5 scenario remain open. CI run 34014293347 for preceding e81c62c is still in progress. No generated keys or synthetic proof artifacts were committed. |

| 2026-09-06 | M1 / CP-005–007 shared authenticated enrollment precondition | Moved retained wallet-signature and original consent-acceptance checks into the shared unrevoked-enrollment loader, requiring an independently supplied chain/registry audience. Issuance-tree construction uses the same checks. Real PostgreSQL regressions reject wrong deployment, forged retained consent and boolean/out-of-interval acceptance times; valid accepted credentials remain eligible after consent expiry. All 19 PostgreSQL integration checks passed with CLI gates enabled. Full Python using the fresh manifest-bound development proof: 776 passed, 65 skipped, 30 warnings; Ruff/diff checks pass. | Partial: this closes a shared-loader authenticity gap, not current authorization. Trusted policy/quote/root enforcement, API/SDK/contract parity and atomic verification/consumption integration remain open. CI run 34014405742 for preceding efc29c0 is in progress. |

| 2026-09-06 | M1 / CP-007 independent current root checks | Added a read-only pilot root verification component requiring separately configured current issuance/issuer/sanctions pins. It validates exact context digests and tenant/deployment/profile, scoped signatures at both evaluation and current clocks, depth-eight trees and the expected credential issuer. Real-signature tests include a different issuer also allowed by the authority, stale head pins, changed context digests and time boundaries. Thirty-three focused root tests passed; full Python with manifest-bound development pairing: 792 passed, 65 skipped, 30 warnings. Ruff/diff checks pass. | Partial: component is not yet wired into current authorization. Proof-signal binding, consistent current-state reads, enrollment/revocation, policy/quote enforcement and atomic consumption remain required. Its result is authenticated root evidence only. CI run 34014542214 for preceding edbc6e9 remains in progress. |

| 2026-09-06 | M1 / CP-006–009 exact credential binding, pilot-transfer-v2 | Identified that v1's transfer-only commitment could not identify which of two valid same-wallet credentials was used, preventing exact durable revocation lookup. Added v2's outer Poseidon commitment to the transfer projection, credential commitment and issuance root, retaining eight signal positions and stable authorization nullifiers. Updated Python/Circom generation, explicit manifest profile selection, current profile routing, signal specification and ADR 0009. Real WASM checks accept either valid credential under its own commitment and reject substitution under the other commitment. Fresh legacy/v2 build and actual v2 pairing/tamper/credential-substitution checks passed with reused development ptau. Existing v1 real pairing remains readable under its original pin; current checks reject v1. Full Python with fresh v2 pairing: 795 passed, 65 skipped, 30 warnings. Ruff/diff checks pass. | Partial: exact-credential statement binding is implemented; current service integration, API/SDK/contracts parity and atomic revocation/consumption remain open. Initial generation started before explicit v2 selection was added and rejected at context binding; the final fresh-directory build passed. Generated keys remain local/unapproved. CI run 34014688043 for preceding 814b91b is in progress; no deployed v2 or production assurance claim. |

| 2026-09-06 | M1 / CP-007 current statement reconstruction and pairing | Connected pinned artifacts, current root signatures/pins, current policy selection, signed valuation and exact credential/transfer projection to read-only pairing inspection. Expectations are reconstructed, not accepted as a caller-supplied vector. Transfer freshness, credential expiry, quote observation age and bounded proof expiry are checked at the verifier clock; roots/quotes also cover evaluation time. Synthetic generation now binds actual signed root digests using public test-only root authority material. Twenty-six focused statement/root tests passed. Fresh legacy/v2 generation and combined real pairing passed, including rejection of another valid credential against the original proof. Full Python with the fresh bundle: 804 passed, 65 skipped, 30 warnings. Ruff/diff checks pass. | Partial: this returns cryptographic inspection, not authorization. Durable enrollment/revocation loading, independently authenticated business facts, consistent state/consumption, API/SDK/contracts parity, encryption and offline historical workflow remain open. Trust is independently reconstructed simulator configuration, not a live source or production approval. CI run 34015021184 for preceding 49bdaf2 remains in progress. |

| 2026-09-06 | M1 / CP-005–007 durable current proof inspection | Added a principal-scoped read-only service that loads authenticated enrollment and revocation state, checks retained root revisions against configured current approvals, and performs reconstructed v2 pairing within one tenant transaction. The synthetic fixture now uses a public test EOA so enrollment runs through the real signature-verifying service. Fresh legacy/v2 generation passed. All 20 PostgreSQL integration tests passed with fresh real proofs and CLI gates enabled, including reconnect, missing roles, foreign tenant, changed signals, root replacement, revocation and unchanged read-only record/consumption counts. Full Python: 804 passed, 66 skipped, 30 warnings; the new database gate was explicitly enabled in the separate PostgreSQL run. CI now couples this test to the circuit job's freshly generated artifacts and PostgreSQL service. Ruff, YAML configuration and diff checks pass. | Partial: inspection remains distinct from authorization/consumption. Business fact authentication, durable policy activation, atomic consumption, trusted encryption, API/SDK/contracts parity and historical export remain open. Tenant-lock serialization is implemented; a concurrent inspection/revocation race test and latest-head remote CI outcome are not claimed. CI run 34015252291 for preceding 621ebfb remains in progress. |

| 2026-09-06 | M1 / CP-005–007 inspection/revocation transaction ordering | Extended the real-proof PostgreSQL gate with revocation and cancellation scenarios. A pause at the pairing-call boundary holds the normal tenant transaction; the test observes an actual ungranted advisory lock for that tenant/database before releasing or cancelling inspection. Successful inspection performs real pairing, revocation subsequently commits, and the next inspection rejects. Cancellation before pairing also releases the transaction. Exact per-kind counts show only revocation plus its idempotency record; consumption remains empty. All 22 database integration checks passed, then the final three inspection scenarios passed after narrowing lock observation to the database. Ruff/diff checks pass. | Resolves the tested inspection/revocation ordering gap, not atomic authorization. No production implementation changed; the full Python suite was not rerun for this test/documentation increment. The initial count assertion incorrectly omitted the legitimate idempotency record and was corrected after inspecting the persistence code. Cancellation of an already running prover is not covered by this scenario. Remaining current authorization, business fact trust, parity, encryption and M4–M5 work stay open. |

| 2026-09-06 | M1 / CP-007 and M2 / CP-010 authenticated external facts | Added domain-separated Ed25519 attestations for external business predicates with exact tenant/transfer/context binding and independently configured deployment/source/predicate/key/time limits. Exact retries deduplicate; conflicting attestations reject independent of delivery order; false/missing facts remain explicit. Attestors cannot assert credential/proof validity, sanctions clearance or valuation authentication. Thirty-six focused attestation/evaluator tests passed with real signatures, including tampering, scope substitution, key rotation, purpose separation and stale clocks. External facts alone still yield INDETERMINATE because derived verification facts are absent. Full Python with real development pairing: 821 passed, 68 skipped, 30 warnings; Ruff/diff checks pass. | Partial: signatures authenticate scoped claims, not their truth or legal sufficiency. Durable signed-evidence retention, authenticated activation, combination with derived verification facts and atomic authorization remain open. CI run 34015640702 for preceding ca2a080 remains in progress. |

| 2026-09-06 | M1 / CP-005 and M2 / CP-010 signed fact retention | Added additive migration 13 and immutable encrypted fact-evidence records, with an explicit facts:ingest principal role. A verified batch is retained atomically; deterministic signed-evidence identities preserve original receiver/time metadata across concurrent retries and reconnect. Current loads recompute identities and reverify scope, freshness and current key authority. All 23 PostgreSQL integration checks passed with real proof/CLI gates enabled, including second-insert rollback, concurrent duplicates, expiry, tenant/role rejection, key removal, invalid signatures, ciphertext privacy and no consumption. Twenty-nine focused principal/attestation tests passed; final full Python with real pairing: 821 passed, 69 skipped, 30 warnings. Ruff/diff checks pass. | Partial: retained evidence is the signed attestation, not its underlying source documents or independent historical authority/timing. Policy activation and atomic authorization integration remain open. An initial database run caught the missing new role in Principal; the explicit role was added and final tests passed. CI run 34015833298 for preceding 7603d29 remains in progress. |

| 2026-09-06 | M1 / CP-007 and M2 / CP-010 combined policy inspection | Added read-only policy evaluation within the same tenant transaction as retained attestation, enrollment/revocation, current root and real proof checks. Only successful inspection creates derived credential/sanctions/valuation/proof facts; failed pairing produces no policy decision. Shared fact loading avoids nested transactions. Real-proof tests cover missing external facts, complete facts without a decisive rule, signed negative counterparty evidence, tampered proof and unchanged write/consumption counts. Signals are snapshotted before awaits; a caller-mutation regression confirms derived expiry stays bound to the verified vector. Final PostgreSQL suite: 24 passed. Full Python before the final snapshot adjustment: 821 passed, 70 skipped, 30 warnings; the final adjustment is covered by the repeated real database/pairing suite. Ruff/diff checks pass. | Partial: composition is verified, not ALLOW/enforce/consume. Durable policy activation, authorization persistence/consumption, complete parity, trusted encryption and historical evidence remain open. No current proof or policy acceptance is inferred from a retained signature alone. CI run 34016093291 for preceding 0350ae6 remains in progress. |

| 2026-09-06 | M1 / CP-005 and M2 / CP-010–011 durable policy activation | Added explicit policy:activate permission, additive migration 14 and encrypted activation revisions separate from reviewed policy approvals. Activation requires a retained effective approval, expected head revision, server actor/time and predecessor digest; idempotent retries return the original receipt without selecting again. Current reads validate tenant/scope/head/effectiveness. All 25 PostgreSQL integration checks passed with real proof/CLI gates enabled, including review without activation, activation before review, competing selections with one winner, preserved history, explicit reviewed rollback, reconnect, expiry and role/tenant rejection. Full Python with real development pairing: 821 passed, 71 skipped, 30 warnings; Ruff/diff and migration-order checks pass. | Partial: activation service is not yet wired into current statement inspection/consumption. Operator-authenticated local history is not independent signed source/timing evidence. Atomic authorization, API/SDK/contracts parity, encryption and M4–M5 integration remain open. CI run 34016309469 for preceding 43ac4b0 remains in progress. |

| 2026-09-06 | M1 / CP-007 and M2 / CP-010 active policy inspection | Durable proof inspection now reads the reviewed activation head inside the same tenant transaction as credential/revocation, roots and real pairing. It requires exact agreement with the independently configured policy pin and activation by the proof evaluation time. All 26 PostgreSQL integration checks passed, including review without activation, review without changing selection, supersession rejection, later rollback rejection after reconnect, real proof success and unchanged inspection/consumption counts. Full Python with development pairing: 821 passed, 72 skipped, 30 warnings; Ruff/diff checks pass. | Partial: this closes current activation integration for read-only inspection, not atomic authorization or independent historical authority. Complete API/SDK/contracts parity, trusted recipient encryption and M4–M5 remain open. The first database run caught missing required tenant/transfer fields in the new empty-facts test fixture; corrected before passing runs. CI run 34016562081 for preceding 6faa2d6 remains in progress. |

| 2026-09-06 | M1 / CP-005, CP-007–008 atomic local authorization | Added a separate authorization service sharing transaction-aware current inspection/evaluation. A real proof and policy ALLOW are required before exact proof bytes, transfer/context, evidence references and decision are retained encrypted with a receipt and unique nullifier consumption. Actor-bound idempotency returns historical receipts without another spend. All 27 PostgreSQL integration checks passed, including a newly generated synthetic ALLOW-policy proof using the existing inspected development keys, missing/negative evidence, invalid pairing, missing role, injected failure after consumption with complete rollback, competing different keys with one winner, concurrent exact retries, changed-request rejection, reconnect/expired retry, exact-byte recovery and read-only inspection after consumption. Full Python with development pairing: 821 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: local authorization now commits atomically; no recipient envelope delivery, public authorization API, payment or on-chain execution, complete API/SDK/contracts parity, or portable historical certificate is implied. These and M4–M5 remain required. Development keys/policy are synthetic and unapproved. CI run 34017016107 for preceding 7ae35c7 remains in progress. |

| 2026-09-06 | M1 / CP-008 trusted recipient encryption in authorization | Added immutable operator-configured recipient key authority scoped to tenant/deployment/beneficiary DID and key validity, with real X25519/HPKE envelope binding to exact transfer/context/proof and recipient metadata. Authorization now requires encryption before atomically retaining proof/envelope/receipt/consumption; identifiers bind randomized ciphertext, not plaintext fingerprints. Ciphertext chunks support a 32 KiB payload within canonical storage limits. Twenty-four new envelope tests cover scoped keys, rotation overlap, validity, low-order key rejection, binding/header tampering, real ciphertext corruption and maximum size. Final PostgreSQL suite: 27 passed, including a newly generated VASP-beneficiary proof, encryption/key failure rollback, changed-payload retry rejection and recipient decryption of the maximum-size retained payload after reconnect. Full Python with development pairing: 845 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: operator inventory is not automatic discovery trust or independent VASP certification. HPKE base mode does not authenticate the sender; opaque payload bytes still require IVMS101/business semantic validation, delivery and acknowledgment. API/SDK/contracts parity and M4–M5 remain open. Initial unit setup exposed string-vs-integer chain representation and was corrected. CI run 34017259323 for preceding dac9a4d remains in progress. |

| 2026-09-06 | M1 / CP-008 bound transfer information validation | Authorization now rejects opaque or malformed payloads before encryption using the explicit clearproof-transfer-information-v1 local profile. It validates bounded natural/legal person names and addresses, preserves Unicode/original bytes, and requires exact party/asset/amount/transfer/context agreement. Duplicate keys, extra/missing fields, unsupported person types and unsafe text reject through a generic error without personal input values. Twenty-three focused checks passed. All 27 PostgreSQL integration checks passed with real development proof authorization, a maximum-size valid UTF-8 payload, invalid/mismatched information rejection without writes, encrypted-only name retention and changed-valid-name idempotency conflict. Full Python: 868 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: explicitly an internal pilot profile, not a complete IVMS101 wire schema or evidence of legal sufficiency. Actual name/address source truth and exact-payload attestation, full standard mapping, bilateral exchange, verifier parity and M4–M5 remain required. Documentation links the reviewed IVMS101.2023 reference and describes the distinction. CI run 34017599149 for preceding 6103470 remains in progress. |

| 2026-09-06 | M1 / CP-006–008 exact private information approval | Added purpose-separated Ed25519 approval of exact payload bytes, transfer/context, credential and source-evidence reference with independent tenant/deployment/source/key/time authority. Atomic authorization verifies the approval before current proof/policy checks and retains it encrypted; receipt and proof identity bind its signature without publishing plaintext fingerprints. Seventeen focused real-signature tests passed for scope/payload/credential substitution, lifetime, rotation/removal, source-reference tampering and purpose separation. All 27 PostgreSQL integration checks passed, including a structurally valid but unapproved name, bad signature, encrypted approval recovery and receipt signature binding after real proof authorization/reconnect. Full Python: 885 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: the signature authenticates an independently configured source claim, not the underlying KYC documents or legal sufficiency. Actual source review/retention, independent historical authority, full IVMS101/bilateral integration, verifier parity and M4–M5 remain open. CI run 34017854448 for preceding 65bdb46 remains in progress. |

| 2026-09-06 | M1 / CP-005, CP-008 and M4 / CP-015 authorization evidence capture | Additive migration 15 introduces immutable encrypted authorization-evidence records. The authorization transaction captures a receipt-bound manifest pinning exact enrollment/policy/activation/root/fact revisions and hashes, plus reconstructable artifact manifest, verification key, full catalog, valuation approval and root-pin bytes. Configuration chunks deduplicate by content; capture rolls back with failed authorization. Local clock and revocation absence are explicitly labeled observations. All 27 real-proof PostgreSQL pilot tests and 32 PostgreSQL storage/migration checks passed, including exact-byte reconstruction, immutable/foreign-tenant rejection, rollback, later activation/revocation and preserved historical selection after reconnect. Full Python: 885 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: this preserves inputs for CP-015; portable encrypted export, authenticated historical decision/timing evidence, independent bundle trust and offline supported/contradicted/indeterminate verification remain open. Captured keys and local absence observations are not trust anchors or global revocation history. Complete verifier parity, bilateral integration and M5 remain required. CI run 34018094385 for preceding 96c97bb remains in progress. |

| 2026-09-06 | M4 / CP-015 encrypted pinned-record export | Added an explicit evidence:export permission and server-approved tenant/reviewer X25519 key scope/validity. Read-only export validates receipt/capture identities, resolves exact retained revisions and hashes, reconstructs captured configuration bytes, and seals a bounded history bundle with tenant/receipt/reviewer/time AAD. No plaintext files, writes or consumption occur. All 27 real-proof PostgreSQL tests passed, including export after later policy/revocation changes, original selection/config preservation, role/scope/expiry/wrong-key/tampering/missing-chunk rejection and unchanged storage counts. A fresh process decrypted the encrypted artifact with the database closed and socket connections disabled. Full Python: 885 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: portable encrypted transport and offline decryption are implemented, not historical verification. Signed decision/timing authority, independent trust configuration, supported/contradicted/indeterminate evaluation and verify-history CLI remain required. HPKE base mode does not authenticate the exporter. Full parity, bilateral integration and M5 remain open. CI run 34018353803 for preceding 673004b remains in progress. |

| 2026-09-06 | M4 / CP-015 offline integrity and pairing inspection | Added a read-only offline inspector requiring reviewer-supplied tenant/receipt and artifact/runtime pins. It checks linked receipt/capture/record/configuration/proof/envelope identities and real Groth16 pairing, with separate integrity and cryptographic results. Altered linked evidence is contradicted; missing evidence is indeterminate; successful pairing remains indeterminate with explicit missing semantics/authority/status/timing reasons. All 27 real-proof PostgreSQL tests passed, including changed receipt/root/policy/key/envelope/signal and duplicate-record rejection, missing-record handling, and an expired-proof review after later activation/revocation. A fresh process decrypts and performs real pairing with the database closed and Python socket connections disabled using independently supplied pins. Full Python: 885 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: this stage cannot return supported. Authenticated historical statement/source/policy reconstruction, decision/timing authority, revocation/compromise evidence and verify-history CLI remain required, along with parity, bilateral integration and M5. Expired-proof pairing is not authorization or proof of historical compliance. CI run 34018599129 for preceding dfe76f5 remains in progress. |

| 2026-09-06 | M4 / CP-015 independently trusted historical statement reconstruction | Added optional reviewer-supplied historical policy, valuation and root authority/pins. Offline inspection reconstructs v2 expectations from captured consent/credential, complete catalog, signed valuation/root records and the claimed authorization time using the existing current-statement implementation. Successful reconstruction is reported separately from pairing; bundle pins cannot approve themselves. All 27 PostgreSQL pilot checks passed with a real expired historical proof, independent original pins, replacement-root and latest-policy rejection. The final focused real-proof authorization/export test also passed after adding runtime-unavailable reporting coverage. Full Python: 885 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: historical business-fact/policy-decision replay, decision/timing authority, revocation/compromise evidence and supported outcome remain required. This reconstructs the circuit statement at a claimed time; it does not authenticate that time or authorize replay. verify-history CLI, parity, bilateral integration and M5 remain open. CI run 34018897397 for preceding 149e61f remains in progress. |

| 2026-09-06 | M4 / CP-015 authenticated-fact policy replay | Offline inspection can now conditionally replay the entire retained policy report after independent statement reconstruction and real pairing. Reviewer-supplied fact authority verifies exact captured fact identities/signatures, tenant/context/source scope and historical validity. Current authorization and history share derived-fact construction. The report records policy_reproduced separately; replay explicitly conditions on the captured local status observation without authenticating it. All 27 PostgreSQL pilot tests passed, including exact report reproduction, fabricated reason-code contradiction despite valid pairing, and excluded fact-source indeterminacy. Full Python: 885 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial: reproduced policy is not historical support. Independent decision authority, authenticated timing/status and compromise evidence, supported outcome and verify-history CLI remain required. Current parity, bilateral integration and M5 also remain open. CI run 34019129745 for preceding 47e8a30 remains in progress. |

| 2026-09-06 | M4 / CP-015 authenticated decision attestation | Local authorization now requires a scoped Ed25519 decision signer and atomically retains its receipt/evidence/context-bound statement inside the encrypted proof. Offline review accepts independently configured signer authority and reports decision_authenticated separately. Key expiry preserves historical signatures; removed keys and known compromise before review remain indeterminate, without trusting a claimed pre-compromise timestamp. Invalid signatures contradict the claim. Ten real-signature unit tests and all 27 real-proof PostgreSQL pilot tests passed, including foreign-signer rollback, retained/exported signature validation and tampering/compromise. Full Python: 895 passed, 73 skipped, 30 warnings; Ruff format/lint and diff checks pass. | Partial: the signature uses an operator clock, not independent timestamp evidence. Authenticated historical status/timing, broader source compromise handling, supported outcome and verify-history CLI remain required. API/SDK/contract parity, bilateral integration and M5 remain open. CI run 34019355015 at preceding 1f8bb13 is in progress. |

| 2026-09-06 | M4 / CP-015 scoped historical registry status | Authorization captures a versioned issuer/credential/registry status observation under the same tenant transaction, bound through the manifest to its decision signature. Separate reviewer status delegation names the authoritative registry, issuer and signer with validity/compromise constraints. Offline inspection reports status_authenticated only after independent statement reconstruction and signature/scope validation; decision trust alone cannot delegate status authority. All 27 real-proof PostgreSQL tests passed after fixing strict base-model conversion, covering export after later revocation, wrong issuer/registry/tenant, compromised signer, changed observation and missing signature. Full Python: 895 passed, 73 skipped, 30 warnings; Ruff format/lint and diff checks pass. | Partial: authentication applies only to the independently approved pilot registry scope, not external/global revocation history. Independent timing, broader source compromise handling, supported outcome and verify-history CLI remain required. API/SDK/contract parity, bilateral integration and M5 remain open. CI run 34019927291 at preceding 3aaaa72 is in progress. |

| 2026-09-06 | M4 / CP-015 independent RFC 3161 timing | Added maintained rfc3161-client >=1.0.8 verification with independently pinned TSA leaf/root/policy, strict response/hash profile, explicit accuracy interval, validity/compromise and decision-window checks. Queries bind the full signed decision through a digest. An authorized service verifies and retains the first response encrypted without modifying the receipt or consuming again; exports include it. Offline review reports timing_authenticated and the timestamp interval separately from operator times. Real OpenSSL tests cover leaf/root/policy replacement, tampering/rebinding, expiry, compromise and insufficient timing. All 27 PostgreSQL tests passed with a freshly timed real development proof, invalid/unauthorized attachment, idempotent retry, restart, export and fresh-process offline pairing/timestamp verification. Thirty-three focused verifier/timestamp tests passed; full Python: 903 passed, 73 skipped, 30 warnings. Fixed a PID-publication race exposed by the first full run using atomic fixture rename; Ruff/diff checks pass. | Partial: the local TSA is synthetic, not production time assurance. A timestamp proves existence within an accuracy interval, not exact decision time or source truth. Historical information/source authority and broader compromise review, supported outcome, verify-history CLI, parity, bilateral integration and M5 remain required. ADR 0010 records standards/library sources and limits. CI run 34020196278 at preceding 5a66b16 completed successfully, including isolated setup/real proofs, durable inspection and all other CI jobs; current change awaits CI. |

| 2026-09-06 | M4 / CP-015 historical information-source approval | Separated shared signature/scope verification from current exact-payload validation. Offline inspection accepts independent information authority and reports information_authenticated, checking the retained approval at decision time and known compromise at reviewer time. Current authorization retains the payload size/hash checks and now rejects known compromised information keys. Twenty-two real-signature unit tests and all 27 real-proof PostgreSQL checks passed, including expiry-safe historical review, altered source evidence with unchanged signature, excluded source and compromise after claimed signing. Full Python: 908 passed, 73 skipped, 30 warnings; Ruff format/lint and diff checks pass. | Partial: authenticates the source assertion, not underlying document truth or independent ciphertext-to-plaintext equality. The authenticated operator receipt binds approval signature and envelope. Broader historical root/valuation/fact compromise review, supported outcome and verify-history CLI remain required, along with parity, bilateral integration and M5. CI run 34063122861 at preceding 8e8f501 remains in progress. |

| 2026-09-06 | M4 / CP-015 complete scoped historical outcome | Added reviewer-time compromise checks for root, valuation and fact keys while preserving decision-time validity. Current checks use the same compromise metadata; duplicate scope entries cannot override a compromised key. Historical reconstruction/replay now apply both clocks. Inspection returns supported only when integrity, pairing, statement, policy, decision, status, RFC 3161 timing and information-source authentication all pass; support is explicitly scoped to the recorded local decision under configured authorities. Fifty-seven focused source-authority tests and all 27 real-proof PostgreSQL tests passed. Tests remove every required trust layer and introduce later root/valuation/fact compromise. A fresh process reconstructs all trust from separate reviewer configuration and returns supported for the expired synthetic bundle after later policy/revocation changes, with database access closed and Python socket connections disabled. Full Python: 911 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial CP-015: supported/contradicted/indeterminate evaluation is implemented, but public verify-history CLI and user-facing trust configuration remain open. Support is not legal certification, independent source-document/ciphertext validation, delivery or settlement. Current parity, bilateral integration, clean-environment pilot and M5 remain required. CI run 34063380878 at preceding 97db33c remains in progress. |

| 2026-09-06 | M4 / CP-015 source verify-history CLI | Added a bounded versioned reviewer configuration and published JSON schema, separate from encrypted evidence. Python command accepts the private reviewer key only through stdin, pins artifacts/runtime independently, and emits minimized scoped JSON with exit codes 0/1/2 for supported/contradicted/indeterminate. Existing Node CLI exposes the command through a shell-free Python handoff. Eleven configuration/input-safety tests passed; CLI TypeScript build passed. All 27 PostgreSQL tests passed with real development proofs, direct Python and built Node commands, database closure/socket-disabled review, wrong-key/missing-trust rejection and encrypted-but-altered receipt contradiction. Full Python: 922 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Local CP-015 command/configuration delivered; clean-environment integrated M0–M5 acceptance and distribution remain open. Broader CLI suite: 12 passed, 1 failed because the existing demo test expects absent bundled compliance.wasm/zkey/vkey files. No unapproved keys were copied into the package and no test was skipped or weakened; repair/document artifact distribution under CP-017. Current verifier parity, bilateral integration and M5 remain required. CI run 34063726905 at preceding 0555c32 remains in progress. |

| 2026-09-06 | M5 / CP-017 legacy artifact availability | Source circuit package now reports missing compiled artifacts explicitly. Legacy demo/prove share complete-set preflight, prefer complete packaged files and accept both flat and compiler layouts; missing/partial/empty/symlinked files fail before proving with an actionable setup diagnostic. Documentation points to the existing isolated development builder and distinguishes legacy 16-signal proofs from v2 authorization. CLI build passed; all 12 CLI and 2 circuit-package tests passed. Built demo/prove missing-artifact commands returned exit 2 with empty stdout. A real demo proof generated and verified using an existing isolated development legacy bundle. npm pack dry run confirmed ten source/metadata files with no compiled keys/WASM. | Partial CP-017: fixes the source availability contract and prior brittle bundled-file assumption; no keys were copied into the package or approved for release. Fresh setup was not repeated for this change. Clean-environment integrated onboarding, artifact provenance/distribution, current verifier parity, bilateral integration and remaining M5 work remain open. |

| 2026-09-06 | M1 / CP-007 authenticated current inspection API | Added POST /pilot/proof/inspect over the existing tenant-transactional current statement service. JWT roles and tenant-scoped operator targets select trusted configuration; request-supplied clocks/roots/keys are rejected. Bounded duplicate-key-safe parsing and generic errors avoid echoing private input. Responses separate pairing validity, development assurance and non-consumption. All 27 real-proof PostgreSQL tests passed, including signed JWT HTTP inspection, failed pairing, wrong signal binding, missing roles, foreign tenant, unknown target/enrollment, malformed/oversized/extra-field input, absent configuration and reconnect; surrounding exact storage counts remain unchanged. Full Python: 922 passed, 73 skipped, 30 warnings; Ruff format/lint and diff checks pass. | Partial CP-007: current read-only HTTP inspection is connected; API authorization/policy evaluation, SDK/CLI and contract parity remain open. Operator targets are explicit in-process configuration restored at restart, not yet a serialized onboarding loader. No business ALLOW, consumption or settlement is inferred. Durable observation workflows, bilateral integration and remaining M5 gates stay open. Prior commit 26215d2 CI run 34064679110 remains in progress. |

| 2026-09-06 | M1 / CP-007 current inspection SDK and CLI | Added thin inspectCurrentProof SDK adapter and inspect-current CLI using shared bounded authenticated HTTP transport. Exact input bytes reach the server; responses require the exact scoped v2 schema and development assurance, rejecting extra fields and unsupported claims. CLI separates pairing true/false from request failure with exit codes 0/1/2 and generic redacted errors. SDK and CLI builds passed; all 145 SDK and 12 CLI tests passed, including transport regression checks. All 27 real-proof PostgreSQL tests passed with the built Node command through SDK, loopback HTTP, JWT and actual pairing, covering valid/invalid pairing, tenant/role rejection and private error redaction; exact surrounding storage/consumption counts remain unchanged. Enabled the built-command gate in the isolated-artifact CI job. Ruff/diff checks pass. | Partial CP-007: this is remote current inspection through the selected trusted API, not independent SDK trust reconstruction or authorization. Full Python was not repeated for this TypeScript/test/documentation change; previous full baseline is 922 passed, 73 skipped. Contract parity, API authorization/policy evaluation, observation workflows, bilateral integration and remaining M5 gates remain open. Public npm 0.3.0 does not contain these source additions. |

| 2026-09-06 | M1 / CP-007 authenticated current policy evaluation API | Added POST /pilot/proof/evaluate over the existing transactional pairing/fact/policy service, sharing bounded input and tenant target selection with inspection. Requires policy:read in addition to inspection/decryption roles and operator-installed fact authority. Requests reference retained signed facts; missing/untrusted/duplicate records reject, while an empty set yields missing-fact evaluation. Responses separate pairing, nullable policy, development assurance and non-consumption. A synthetic deny rule plus signed alternative information/counterparty facts exercise ALLOW, DENY, REVIEW and INDETERMINATE through real JWT/HTTP/PostgreSQL and a freshly generated development proof. All 27 database tests passed, including failed pairing without a decision, wrong authority/tenant/roles, request override rejection, reconnect and unchanged record/consumption counts. Full Python: 922 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. The first negative test incorrectly constructed a forbidden empty authority store; corrected it to use a valid wrong-key authority and reran the database suite. | Partial CP-007: evaluation is read-only and applies to the selected signed fact set; it does not assert exhaustive/latest external observations. Durable observation reports, evaluation SDK/CLI, authorization API and contract parity remain open, along with bilateral integration and remaining M5 gates. Synthetic outcomes are not regulatory validation. Prior 09d957c CI run 34065276190 remains in progress. |

| 2026-09-06 | M5 / CP-016 durable proof observation | Added immutable encrypted observation records under additive migration 16 and a dedicated observations:write role/operation. ProofObservationService evaluates current proof and retained facts in the existing tenant transaction, atomically storing a minimized versioned observation and idempotency result. Mode is fixed to observation, execution is not requested, and no authorization proof/receipt/consumption is created. Full PostgreSQL storage/migration gate: 59 passed. Real-proof checks cover all four policy outcomes, failed pairing without a decision, concurrent exact retries, new logical requests, changed request/actor conflicts, missing write permission, immutability, actual consumption foreign-key rejection, injected post-record rollback, encrypted rows and foreign-tenant reads. After restart and proof expiry, an exact retry under replaced authority returns the original report; the final focused real-proof test additionally confirms a fresh expired request rejects with no added records. Full Python: 922 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial CP-016: this is the durable local observation service, not complete onboarding. Reports are operator-clock encrypted records, not independently signed historical bundles or current authorizations. Observation API/CLI, deterministic bilateral scenarios, coverage/disagreement/latency reporting, clean setup and CP-018 commercial preparation remain open, as do current authorization API and contract parity. Documentation: docs/internal/PILOT_OBSERVATION_MODE.md. |

| 2026-09-06 | M5 / CP-016 authenticated observation API | Added POST /pilot/proof/observe using the bounded current-input preparation and the existing atomic observation service. Dedicated write/inspection/policy/decryption roles are required; mode, clock, consumption and trust overrides reject. Added private-body POST /pilot/proof/observations/read requiring only policy-read/decryption and storage, independent of current target/runtime configuration. All 27 PostgreSQL integration tests passed with real proofs and signed JWTs: four HTTP policy outcomes, concurrent exact retries, changed-request conflict, tenant/role rejection, generic malformed-input errors, bounded references and retrieval after reconnect into an app without current targets. Four logical HTTP observations add exactly eight encrypted observation/idempotency records and zero consumptions. Full Python: 922 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial CP-016: observation creation/read API is connected, but SDK/CLI, pagination, coverage/disagreement/latency reports, deterministic bilateral scenarios and integrated clean onboarding remain open. Current authorization API, contract parity and CP-018 preparation remain required. A stored ALLOW never activates enforcement or settlement. |

| 2026-09-06 | M5 / CP-016 observation SDK and CLI | Added createObservation/readObservation SDK adapters and observation create/read CLI with private stdin and environment bearer tokens. Clients enforce the exact non-enforcing development profile, bounded/sorted fields, nullable-policy/pairing consistency, policy scope/time and canonical record identity; reads bind the requested observation ID. Successful storage/retrieval exits 0 for every policy outcome, never implying authorization; failures exit 2 with generic diagnostics. SDK/CLI builds and all 149 SDK plus 12 CLI tests passed. All 27 PostgreSQL tests passed through the built clients; the final focused real-proof test additionally creates a fresh DENY observation via CLI, retries it exactly, reads all outcomes, checks Python/TS digest parity and confirms only the additional observation/idempotency pair is written with no consumption. Tenant/role/conflict/input errors are redacted. Ruff/diff checks pass. | Partial CP-016: clients are connected to the trusted API, not independent historical authorities. Full Python was not repeated for this TypeScript/test/documentation increment; previous full baseline is 922 passed, 73 skipped. Pagination, coverage/disagreement/latency reporting, deterministic bilateral scenarios, clean onboarding, authorization API/contract parity and CP-018 preparation remain open. Public npm 0.3.0 does not contain these source clients. |

| 2026-09-06 | M5 / CP-016 selected-cohort reporting | Added a bounded explicit cohort model and read-only POST /pilot/proof/observations/report. Immutable tenant records are decoded with the same identity checks as single reads; no current target is needed. Reports distinguish observed/missing/failed-pairing/policy/determinate counts, all four outcomes, distinct observed transfers and baseline comparisons with explicit denominators. Caller labels are marked unverified; missing or failed-pairing cases never become outcomes or automatic disagreements. Duplicate case/observation IDs reject; normalized cohort digests and sorted results are deterministic. Two focused denominator/duplicate tests and all 27 real-proof PostgreSQL tests passed, including expected six-case counts, input-order independence, role rejection, foreign-tenant unavailability, bounded/private errors and unchanged storage/consumption counts. Full Python: 924 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial CP-016: coverage applies only to selected cases, not an inferred population; baseline labels do not establish external truth or disagreement causes. Latency is explicitly not-recorded because v1 observations lack duration measurements. Latency instrumentation, report SDK/CLI, broader cohort discovery/pagination, bilateral scenarios, clean onboarding, authorization API/contract parity and CP-018 preparation remain open. |

| 2026-09-06 | M5 / CP-016 versioned evaluation-duration measurements | New observation v2 records bind bounded monotonic nanosecond durations under a new digest domain, scoped to current evaluation inside the tenant transaction. Upload/auth/preflight, lock waiting, retention and response time are excluded. Version routing preserves v1 bytes/IDs and cached v1 retries; no record migration or circuit/artifact change is needed. SDK/CLI supports both versions with matching digest and duration validation. Cohort report v2 adds measured/unmeasured coverage and exact integer total/min/max; missing and v1 observations do not become zero samples. Three focused Python tests, all 150 SDK and 12 CLI tests, SDK/CLI builds and all 27 real-proof PostgreSQL tests passed. Database checks retain a v1 observation/idempotency pair, retrieve it after expiry under the new writer, verify mixed-version duration coverage, and validate new timed records through built clients. Full Python: 925 passed, 73 skipped, 30 warnings; Ruff/diff checks pass. | Partial CP-016: measured durations describe selected completed local evaluations, not end-to-end latency, independent timestamps or production performance. Update v1-only clients with new writers; legacy records remain readable. Report SDK/CLI, cohort discovery/pagination, deterministic bilateral scenarios, clean onboarding, current authorization API/contract parity and CP-018 preparation remain open. |

| 2026-09-06 | M5 / CP-016 cohort report SDK and CLI | Added typed reportObservationCohort SDK support and observation report CLI. Clients bind cohort ID/digest, normalized case identities and baseline labels to the request, validate exact response profiles, recompute case/count/denominator consistency and check bounded latency coverage/totals while preserving unknown measurements as null. CLI success remains report retrieval, not authorization or agreement. SDK/CLI builds, all 153 SDK and 12 CLI tests, and all 27 real-proof PostgreSQL tests passed. The built command reproduces the real API's mixed observed/missing cohort report and foreign-tenant unavailability, with role/input redaction and unchanged record/consumption counts. Initial Vitest collection failed on the machine's temporary-directory quota before running tests; rerunning with TMPDIR=/home/agent/.cache/clearproof-tests passed. Ruff/diff checks pass. | Partial CP-016: selected-cohort clients are delivered. Distinct-transfer and timing values remain API-supplied facts checked for consistency, not independently authenticated sources. Full Python was not repeated for this TypeScript/test/documentation increment; prior full baseline is 925 passed, 73 skipped. Cohort discovery/pagination, bilateral scenarios, complete clean onboarding, current authorization API/contract parity and CP-018 preparation remain open. |

| 2026-09-06 | M5 / CP-018 paid-pilot preparation and usage inventory | Created docs/commercial packet: scoped offer, customer interview script, baseline/outcome worksheet, explicitly hypothetical pricing experiment and documented free/paid distribution boundary. Capability claims follow current source/plan; no customer outreach, quote, commitment or willingness-to-pay claim was made. Added GET /pilot/usage with dedicated usage:read permission and one SQL snapshot of tenant record/ciphertext inventories and consumed nullifiers. Counters are metadata-only, versioned and explicitly not invoices; retry, revision, retention and deduplication semantics are documented. All 28 real-proof PostgreSQL pilot tests passed, including the new empty/count/concurrent-retry/conflict/reconnect/foreign-tenant/API-permission/read-only inventory checks. Full Python: 925 passed, 74 skipped, 30 warnings; Ruff/diff checks and packet relative links pass. | CP-018 local preparation delivered. Pricing cards are research inputs, not validated market prices or offers; inventory is not a billing ledger or adoption metric. F4 customer access/commercial validation and F5 production assurance remain open. M0–M5 overall remains incomplete: current authorization API/contract parity, bilateral scenarios, cohort discovery/pagination and the reproducible clean-environment integrated pilot still require work. |

| 2026-09-06 | M5 / CP-016 observation discovery and pagination | Added private-body POST /pilot/proof/observations/list with bounded exclusive-ID keyset scans, policy-read/decryption permissions and canonical identity/tenant checks on returned records. Source SDK listObservations and observation list CLI validate profile, selectors, record identities, strict ordering and continuation. Discovery preserves legacy v1 and timed v2 observations, works without current targets and does not write or consume. SDK/CLI builds pass; 156 SDK and 12 CLI tests pass. All 28 PostgreSQL pilot tests pass, including real API/built CLI traversal, complete quiescent scan equivalence, repeated pages, end/empty/foreign pages, mixed versions, denied permissions, malformed/oversized selectors and unchanged storage/consumption counts. Full Python: 925 passed, 74 skipped, 30 warnings; Ruff/diff checks pass. | Local discovery/pagination delivered. This is a live retained-record scan, not a chronological feed or snapshot across requests; concurrent insertion before a cursor can be missed. Explicit reviewed case IDs define reproducible cohorts. Current authorization API/contract parity, deterministic bilateral scenarios, artifact distribution and the clean-environment integrated pilot remain open; M0–M5 is not complete. |

| 2026-09-06 | M1 / CP-005–CP-009 explicit authorization API and clients | Added POST /pilot/proof/authorize with independent consume/generate/inspect/policy-read/decryption checks, bounded private proof/fact/idempotency input and a separate tenant-scoped operator target inventory. Exact transfer information and its source approval are provisioned as authenticated encrypted input bound to tenant/target; 32 KiB payloads use bounded canonical base64 chunks. No request can select PII, recipient authority, signer, trust inventory or clock. The existing atomic service performs new authorization; the response is explicitly a recorded local receipt and may be an expired same-key recovery. Added authorizeCurrentProof SDK and authorize-current CLI with canonical receipt identity, request nullifier/expiry and existing padded-base64url recipient fingerprint checks. Uncertain response errors instruct same-request/key recovery, never automatic replacement keys. SDK/CLI builds and 159 SDK/12 CLI tests pass. Two input encryption/scope/rotation/bounds tests pass. All 28 PostgreSQL pilot tests pass: real proofs, competing HTTP consumption, exact direct/API/CLI retries, reconnect/expired recovery, role/tenant/request rejection, tampered/relabelled encrypted input, no writes on rejection, retained envelope/evidence checks and offline historical review. Full Python: 927 passed, 74 skipped, 30 warnings; Ruff/diff checks pass. | Local authorization API/SDK/CLI path delivered; it neither transmits an envelope nor executes funds. Operator target provisioning/persistence remains part of clean onboarding; lost target input prevents this endpoint from reconstructing a retry. Initial size and recipient-fingerprint integration failures were corrected without relaxing canonical/envelope formats. Contract acceptance parity remains open: PilotRootCheckpoint publishes roots but does not verify proofs. Deterministic bilateral scenarios, artifact distribution and the complete reproducible clean-environment pilot still gate M0–M5 completion; F1–F5 remain explicit follow-ons. |

| 2026-09-06 | M1 / CP-007 eight-signal contract pairing | Added PilotGroth16Verifier with constructor-fixed affine verification key, explicit development assurance, artifact metadata and ABI key commitment. It reuses the existing licensed Pairing equation/library, validates key curve encodings, bounds every proof coordinate before negation, bounds all scalars and enforces exactly eight public signals. No development key constants were committed. Three real-EVM tests pass using the same development proof/public signals as the passing Python current-statement pairing gate; snarkjs agrees on the valid proof, each of eight signal mutations and a changed valid curve point. Malformed points, scalar aliases, legacy ABI length, invalid keys and changed key commitments reject as expected. Solidity compile and focused strict TypeScript checking pass. The isolated development builder now runs this EVM gate after its fresh Python proof check; fresh setup was not repeated locally this turn. TypeChain regenerated the new bindings and refreshed stale ComplianceRegistry bindings from the current ABI; no generated files were edited manually. Ruff/diff checks pass. | Partial CP-007: this is pairing parity, not current statement/authorization parity. The contract integration must still authenticate/reconstruct expected context, current roots/enrollment/revocation/policy/valuation/participants and replay state. Broader Hardhat run: 63 passed, 1 failed in the legacy E2E witness (local root WASM expects 124 inputs, fixture supplies 123). Whole-workspace TypeScript checks also expose legacy declaration/deployment-script issues; those checks are not green and remain required repair under CP-017. No tests were disabled or assertions weakened. Bilateral scenarios and complete clean-environment onboarding remain open; M0–M5 and follow-on gates remain uncompleted. |

| 2026-09-06 | CP-017 contract validation and deployment preparation repair | Contract typechecking now includes regenerated TypeChain bindings and the shared snarkjs declaration, and runs as a package command after Hardhat tests in CI. Corrected legacy deployment/gas scripts to use the router, selector, registries and all threshold constructor arguments. Shared prepareLegacyVerifier preserves the 24-hour timelock and deployment records explicitly retain pending activation metadata; explorer constructor arguments match actual deployment. Both deployment entrypoints pass local subprocess tests with isolated output directories, actual network names and no local explorer publication; the gas benchmark is ephemeral-Hardhat-only and runs successfully. The legacy E2E test accepts an explicit complete artifact bundle, generates/compiles its matching licensed verifier in a cleaned temporary directory, and completes real proof generation, off-chain verification, on-chain submission and read-back. The isolated development builder now enables both legacy and pilot EVM gates using its own fresh outputs. Full contract suite with both existing isolated development bundles: 66 passed. Whole-workspace package typecheck, Ruff and diff checks pass. | The prior legacy witness failure was resolved for reproducible validation by selecting the coherent development bundle, not by changing assertions or overwriting root artifacts. The old root artifact location remains a compatibility default and can still contain incompatible local files. Fresh setup was not repeated locally this turn; the new builder stages are wired for the fresh-artifact CI job. No remote network deployment, activation or explorer publication occurred. This closes the observed contract test/typecheck failures for the documented configuration, not CP-017 or M0–M5 overall: current authorization contract parity, bilateral scenarios and complete clean-environment onboarding remain required. |

| 2026-09-06 | M1 / CP-007 current-state contract checkpoint mechanics | Added PilotCurrentRegistry around the fixed eight-signal verifier. Tenant publishers attest immutable context/transfer/projection bindings and typed root, credential, policy, valuation, participant and authorization heads. Exact revision/digest/epoch checks invalidate superseded or disabled state; issuer/sanctions values, evaluation/expiry, chain and registry audience are checked before pairing. Inspection is read-only and distinct from ALLOW; consumption requires an exact current ALLOW checkpoint, the designated caller and an unconsumed tenant/nullifier. Publisher replacement invalidates old heads/statements without clearing consumption. A synthetic-only helper generates a real proof for the deployed registry address and verifies it through Python current-statement inspection before the EVM uses it. Thirteen real-proof registry tests pass, including state supersession/restoration, disabled credential/approval, missing/non-ALLOW approval, caller/tenant/domain/proof rejection, expiry, publisher replacement and replay. Full contract suite: 79 passed; package typecheck passes. Full Python: 927 passed, 74 skipped, 30 warnings; Ruff/diff checks pass. TypeChain bindings were generated normally. The fresh-artifact builder now invokes this suite with its own Python interpreter. | Partial CP-007: participant and ALLOW heads are synthetic test labels, not independently verified source approvals. ADR 0006 permits trusted binding attestations, but the actual publisher bridge must validate durable enrollment/revocation, policy activation, signed facts, quotes, information and recipient evidence and enforce source-specific rules before publishing. The registry's consumption map is not atomically synchronized with PostgreSQL; an authoritative consumption path must be selected and enforced. Current authorization parity is not complete. Bilateral scenarios, clean-environment end-to-end onboarding and F1–F5 remain open. |

| 2026-09-06 | M1 / CP-007 authenticated mirror preparation and replay ownership | Selected PostgreSQL as the pilot authorization authority. PilotCurrentRegistry now exposes receipt-bound mirror rather than independent consume, requires the exact receipt digest in its current ALLOW head and retains one receipt per tenant/nullifier. Added AuthorizationMirrorService.prepare: tenant-locked, permissioned authentication of retained receipt/proof/envelope identities, exact consumption-to-proof association, signed decision/information, recipient binding and current enrollment/root/policy/fact/valuation/pairing ALLOW. Output contains minimized head candidates and proof data, no PII/envelope/source signatures, assigns no chain revisions and performs no writes/RPC. Authenticated fact/quote cutoff helpers preserve signed expiry and enforce inclusive observation-age limits; candidate windows also respect proof/source/policy/recipient expiry. Participant and receipt checkpoints may become current after proof evaluation. All 28 real PostgreSQL tests pass, including preparation stability across reconnect, unchanged records/consumption, missing consumption, source-age boundary, invalid information/signer, permissions and expiry. Full Python: 929 passed, 74 skipped, 30 warnings. Full contracts: 80 passed, including receipt mismatch/replay and later-issued evidence. Contract typecheck and Ruff/diff checks pass; TypeChain generated normally. | Partial CP-007: ownership is now explicit and the local preparation path authenticates actual retained evidence. The chain writer, revision/epoch reconciliation, uncertain-publication recovery and shared PostgreSQL-to-EVM adversarial tests remain open. A preparation deadline is not a guarantee that source/trust state stays unchanged; publication must revalidate and superseded checkpoints must be invalidated. No RPC publication, production deployment or fresh trusted setup occurred. Deterministic bilateral scenarios and complete clean-environment integrated onboarding still gate M0–M5; F1–F5 remain open. |

| 2026-09-06 | M1 / CP-007 atomic checkpoint publication prerequisite | Added publishBatch to PilotCurrentRegistry: one publisher transaction selects all eight heads and publishes their statement, checks the expected publisher epoch and every preceding revision, and validates exact scope/digest/resulting revision pins. Unchanged heads can be reused only with matching values/intervals and current epoch. Any late checkpoint or statement failure rolls back all earlier head changes. Added statementPublication for deterministic ID/epoch read-back after an uncertain send; documented that historical existence does not prove current validity, finality or mirroring. Three additional real-EVM tests cover successful reuse/publication/mirror, duplicate publication rejection, atomic rollback, unauthorized callers, mismatched reused values and stale/replaced publisher epochs. Full contract suite with both isolated development artifact bundles: 83 passed; whole-contract typecheck and diff checks pass. TypeChain bindings regenerated from the ABI. | This closes the partial-chain-update hazard for the future publisher writer, not the complete source-to-chain bridge. Durable write intent, pre-broadcast source revalidation, pinned-chain reconciliation/finality and shared PostgreSQL-to-EVM adversarial tests still require implementation. No remote deployment, publication or production artifact claim. Python services were unchanged; full Python was not repeated (last baseline 929 passed, 74 skipped), and the real PostgreSQL baseline remains 28 passed. Bilateral scenarios and clean integrated onboarding remain open; the M0–M5 goal is active. |

| 2026-09-06 | M1 / CP-007 and CP-017 consumed PostgreSQL receipt to real EVM gate | Extended the real authorization fixture with an explicitly enabled loopback EVM. It deploys the matching verifier/current registry before generating the proof, so the proof binds the actual deployment address. With the database still live, it re-prepares the consumed receipt at the current clock and publishes all eight authenticated head candidates and the statement in one batch. Real EVM inspection, exact receipt mirroring and fresh-client read-back pass; wrong receipt/caller, replay and disabled-checkpoint inspection reject while mirror history remains. The fixture subsequently completes the existing API/CLI, historical evidence and database checks, including exactly one consumption and unchanged retained-record counts. Added scripts/test_pilot_mirror.py to own node startup/readiness/cleanup and run the full PostgreSQL suite, using separate test schemas. The fresh-artifact CI job now invokes this runner. Focused joined gate: 1 passed; full runner: 28 passed, 6 warnings; full Python: 929 passed, 74 skipped, 30 warnings. Ruff, workflow parse/wiring and diff checks pass; no owned Hardhat node remains. | This replaces synthetic participant/ALLOW labels with actual retained signed-source evidence in the joined local gate. It is not the durable publication writer: retained write intent, uncertain-broadcast recovery, pinned runtime/finality checks and automated source invalidation remain open. The disabled-checkpoint test explicitly submits invalidation and does not claim a live revocation relay. Existing inspected unapproved development artifacts were reused locally; fresh setup/remote CI was not claimed complete. No remote deployment or publication occurred. Bilateral scenarios and complete clean-environment onboarding still gate M0–M5; F1–F5 remain open. |

| 2026-09-06 | M1 / CP-007–009 durable signed publication intent and single broadcast claim | Added encrypted PublicationJournal and migration 17. Bounded signed EIP-1559 transactions are independently decoded/recovered and checked against configured chain, registry, sender, zero value and exact calldata digest; their hash comes from retained bytes. Reservation requires an existing exact consumed ALLOW receipt, enforces receipt expiry, stores encrypted transaction chunks, and reserves account nonces across tenants plus one phase per tenant/receipt. Exact reservation retry is stable. The journal revalidates through the writer callback before committing a one-time broadcast claim; crashes, exceptions, cancellation, wrong returned hashes and successful responses never clear that claim or trigger an automatic second send. Inspection survives reconnect/expiry and deliberately reports chain outcome not-established. The joined EVM gate now signs/retains its actual publish and mirror transactions; it deliberately loses the publication response after node acceptance, reconnects PostgreSQL, recovers the retained hash, finds the mined transaction and verifies registry state without another send. Seven signature/profile tests pass; three PostgreSQL journal tests cover global nonce/phase conflicts, encryption, reconnect, foreign reads, competing claims, stale revalidation and uncertain responses. Full owned-node PostgreSQL runner: 31 passed, 6 warnings. Real proof-storage migration/regression suite: 32 passed. Full Python: 936 passed, 77 skipped, 30 warnings. Ruff/diff checks pass; CI storage and fresh-EVM gates include journal tests. | Partial writer delivery: the journal is a storage/broadcast boundary, not an independent source or finality authority. Its caller still supplies reviewed phase/statement/calldata/plan/runtime bindings and current source/chain validation. Chain canonicality/finality, pending/replaced/orphaned classification, pre-send-crash reconciliation, controlled replacement after failed publication and automated source invalidation remain open. Global nonce exclusion applies to this database, not external account users. Existing usage inventory excludes journal rows/ciphertext; it is not billing. No production or remote publication occurred. Bilateral scenarios and complete clean-environment onboarding still gate M0–M5; F1–F5 remain open. |

| 2026-09-06 | M1 / CP-007–009 pinned read-only publication reconciliation | Added PublicationReconciler with separately configured chain/address/runtime pins, explicit latest/safe/finalized anchor, minimum confirmations and block-age policy. It compares retained transaction hash, recovered account nonce, sender, destination, chain, value and calldata with RPC data; verifies receipt/inclusion identity, canonical block membership, runtime at anchor/inclusion, exact registry event and inclusion-time state; then rereads headers/receipt to reject changing observations. Reports distinguish not-found, pending, noncanonical, awaiting-confirmations, confirmed-success and confirmed-failure. Unsupported finality tags do not fall back. Every result leaves current authorization not-evaluated and resubmission not-authorized; no journal writes or RPC sends occur. Journal inspection now exposes the authenticated account nonce for reconciliation. Twenty-five focused tests pass, including wrong transaction/event/tenant/statement/receipt/nullifier/state, missing receipt, stale observation and anchor reorg. The actual PostgreSQL/EVM publish and mirror paths both pass not-found before send, awaiting-confirmations at one block and confirmed-success with exact state at two latest blocks after lost-response recovery. Full owned-node PostgreSQL runner: 31 passed, 6 warnings. Full Python: 961 passed, 77 skipped, 30 warnings; Ruff/diff checks pass. | This delivers observation and confirmation-policy checks, not consensus light-client verification: the provider remains trusted for chain data, latest depth is explicitly not consensus finality, and effects describe inclusion-time state. Durable worker outcome transitions, replacement-hash discovery, pre-send-crash resolution, controlled failed-publication replacement and automatic source invalidation remain open. No result releases a reserved nonce or authorizes a second transfer. Existing unapproved development artifacts were reused; no remote publication or fresh-setup assurance was claimed. Bilateral scenarios and complete clean-environment onboarding still gate M0–M5; F1–F5 remain open. |

| 2026-09-06 | M1 / CP-007–009 durable reconciliation history | Added PublicationRecoveryService and encrypted PublicationHistory with migration 18. Fresh reconciliation precedes persistence; RPC/validation failures and changed or mismatched confirmation policy produce no history entry. Typed observations enforce phase, inclusion completeness, confirmation arithmetic, execution/effect consistency and no authorization/resubmission claim. Entries bind the retained transaction, policy digest, observation clock, tenant, sequence and previous observation ID. Appends serialize under the tenant lock; exact latest retries are stable, older clocks reject, and later weaker results remain separate records after prior success. Bounded sequence pages authenticate encrypted identities and predecessor links across reconnect and explicitly leave current chain state not-established. PostgreSQL tests cover retry, encryption, pagination, reconnect, foreign isolation, substitution/status/clock/policy rejection, and confirmed-success followed by not-found. The actual EVM publish and mirror paths both retain not-found, awaiting-confirmations and confirmed-success history across journal reconnect. Full owned-node PostgreSQL runner: 34 passed, 6 warnings. Real storage migration/regression tests: 32 passed. Full Python: 961 passed, 80 skipped, 30 warnings; Ruff/diff checks pass. | Durable observation history is delivered, not action permission: it never clears a broadcast claim, releases a nonce, sends a transaction or treats old success as current state. The caller must retain the independently configured policy corresponding to each stored digest. Controlled replacement, resolving a pre-send crash, replacement-hash discovery and automated invalidation/worker action transitions remain open. History/journal rows remain outside existing usage counters. No remote publication or production assurance claim; existing unapproved artifacts were reused. Bilateral scenarios and clean-environment integrated onboarding still gate M0–M5; F1–F5 remain open. |

| 2026-09-06 | M1 / CP-007–009 explicit recovery after a pre-send crash | Added bounded recovery of the exact retained signed transaction through PublicationRecoveryService.rebroadcast_missing. Fresh pinned reconciliation must report not-found; the original intent must remain within its window and source revalidation must pass. Latest runtime and canonical/pending account nonce must match, simulation of the original call must succeed, and header/policy must remain stable. Migration 19 preserves existing claims as attempt one and enforces a consistent counter capped at three total attempts. The next attempt is claimed transactionally using the expected counter before sending identical bytes; no new signature, fee, nonce, calldata or transfer authorization is created. Failed preflight does not consume an attempt; uncertain send/cancellation preserves it. The actual EVM fixture now covers a crash before sending the mirror, database reconnect, explicit recovery of the same hash, successful inclusion and refusal to retry included transactions; the prior lost-publication-response case remains. Owned-node runner at that checkpoint: 35 passed, 6 warnings. Expanded focused PostgreSQL preflight/journal plus storage regressions: 47 passed, including known transaction, nonce/runtime/simulation/expiry/source/reorg rejection, concurrent retry and attempt cap. A subsequent dedicated migration-upgrade test passes with existing claimed/unclaimed records preserved. Full Python: 961 passed; database-only recovery checks were exercised explicitly. Ruff/diff checks pass. | The bounded pre-send recovery gap is closed for identical signed bytes under the documented fresh checks. A read-only observation alone still never authorizes resubmission. Exhausted attempts require operator review; unknown replacement-hash discovery and fee/nonce replacement are unsupported. Preflight is not atomic with execution, so contract epoch/revision/expiry checks remain necessary. Complete publisher orchestration, automatic source invalidation, bilateral scenarios and clean-environment onboarding still require scope/completion review against CP-001–018. No remote publication or production assurance claim; existing unapproved development artifacts were reused. M0–M5 remains active and F1–F5 remain open. |

| 2026-09-06 | M3/M5 / CP-013 and CP-016 local bilateral semantics and six scenarios | Re-read the original milestone acceptance requirements and prioritized the explicitly required bilateral scenarios over additional unrestricted transaction replacement features. Reviewed current OpenVASP core specification (3.2.1, master raw source) and extension behavior; corrected legacy TRPBridge documentation that claimed empty-person, symbol-mapped extension JSON was automatically compatible. Added TRPBridge.build_pilot_request and an explicit clearproof-local-bilateral-v1 counterparty. Supported-profile dispositions require an independently trusted signed originator receipt, exact authorized-proof/envelope bindings, current recipient authority/private key, successful HPKE decryption, mandatory private transfer-information semantics and signed information approval. Deterministic accept/reject/request-information, pending/timeout, key-overlap/retirement and unsupported-version cases run from the actual real-proof PostgreSQL authorization record. Missing/substituted message components, expired receipts and missing/retired keys reject without fallback; repeated results and minimized output are checked. The surrounding gate preserves record/consumption counts. Full owned-node PostgreSQL/EVM runner: 44 passed, 6 warnings. Full Python: 961 passed, 90 skipped, 30 warnings; Ruff/diff checks pass. Protocol review and boundaries are documented in PILOT_BILATERAL.md. | Programmatic local scenarios delivered, not live TRP interoperability. The simulator is explicitly in-process and uses the authenticated originator decision for originator proof/policy approval; it does not make a new authorization or execute funds. Persistent exchange-event ingestion, combined custody/counterparty investigation reports, scenario/CLI packaging and the clean-environment end-to-end pilot remain open under CP-013/016/017. Completion still requires the original M0–M5 acceptance gates; unsupported fee/nonce replacement is an operational limitation rather than a new unrestricted local-pilot requirement. F1–F5 remain open; no external counterparty communication or production assurance claim occurred. |

| 2026-09-06 | M3/M5 / CP-012–016 durable local exchange and combined investigation | Added LocalExchangeService to load the tenant authorization record, validate its local counterparty message and atomically retain minimized response evidence, event/index and idempotent result. Shared event ingestion now supports the existing transaction without nested persistence. Delivery identity binds receipt, source, sequence, declared observation clock, context and configured behavior. Concurrent exact retries retain one result; changed requests conflict. The actual real-proof PostgreSQL fixture delivers acceptance before an earlier information request, verifies deterministic replay and reconnect, rejects new deliveries with unavailable recipient keys and preserves exact historical retries. Duplicate/reordered custody-simulator events join the same investigation, retaining the missing-finality finding and exactly one authorization consumption. Pending and unsupported-version now produce queue findings. Full owned-node PostgreSQL/EVM runner: 44 passed, 6 warnings. Full Python: 961 passed, 90 skipped, 30 warnings; the subsequently extended reconciliation suite independently passed all 20 cases. Ruff lint/format and diff checks pass. | Durable local exchange/report integration delivered. Observed time is simulator-declared, results are not signed remote responses, and exact retry is historical retrieval rather than current acceptance. Custody events in this joined fixture are internal simulations, not live Fireblocks deliveries. Scenario/CLI packaging, complete clean-environment onboarding and the final original-requirements acceptance audit remain open. Existing unapproved development artifacts were reused; no external communication, remote publication or production assurance claim. M0–M5 remains active and F1–F5 remain open. |

| 2026-09-06 | M5 / CP-016–017 executable local counterparty CLI | Added source CLI counterparty command and Python entry point with separate bounded request/public-trust files, explicit simulation clock/behavior/deadline and bounded stdin private-key JSON. Public configuration pins expected transfer/context and decision/information/recipient authorities independently of the message. Output distinguishes local disposition from authorization or execution; valid rejection/timeout are disposition success, invalid input exits 2 with minimized errors. Extracted the shared bounded regular-file reader from history_cli without changing its behavior. The actual real-proof PostgreSQL fixture invokes the built command for accept/reject/request-information, pending/timeout, unsupported version, overlapping/retired keys, expiry, missing keys and malformed input; it checks empty stderr and secret/input redaction. The initial gate exposed history-import parser warnings, then an incorrect hexadecimal constraint on base64url recipient IDs; both were corrected. Final owned-node PostgreSQL/EVM runner: 44 passed, 6 warnings. CLI build and all 12 CLI tests pass. Full Python after the reader extraction: 963 passed, 90 skipped, 30 warnings; the subsequent key-ID correction was verified by the final real-envelope CLI gate. All 11 focused history CLI tests and final Ruff lint/format/diff checks pass. | Counterparty CLI scenarios delivered using actual local authorization evidence. The command itself is read-only and in-process; durable event recording remains LocalExchangeService. No network interoperability, production setup or new transfer authorization is claimed. Fresh-checkout service/setup automation, retained demo reports/export packaging and the original M0–M5 completion audit remain open. Existing unapproved development artifacts were reused; F1–F5 remain open and the full goal remains active. |

| 2026-09-06 | M5 / CP-017 retained acceptance reports and offline export | Extended scripts/test_pilot_mirror.py with explicit --output to a new mode-0700 directory. Successful synthetic fixtures retain nine bounded-scope outputs: reviewed policy comparison, four decision observations, coverage/disagreement/latency cohort, CLI counterparty dispositions, durable investigation, encrypted history export, independent public reviewer configuration, review clock and offline report. The generated synthetic reviewer decryption key is kept separately under private/ with mode 0600; no private keys are included in report artifacts. Completion requires the exact report inventory and private companion; failures leave no success manifest. The manifest inventories byte counts/SHA-256 and the development artifact pin, explicitly leaving clean_environment not-established. CI invokes capture and uploads only reports/ plus run.json. Added operations/local-pilot-acceptance.md with the actual runner and offline reproduction example. Full owned-node PostgreSQL/EVM capture run: 44 passed, 6 warnings. All nine inventories, protected report/key modes, reviewer-key and synthetic-person-name exclusion checks pass. The documented code independently reproduces the exact retained historical report with socket connections disabled after the test process exits. Missing-output rejection passes; final manifest mode-0600 and refusal-to-overwrite checks pass using copies of actual run outputs. Workflow YAML and explicit upload-path checks, Ruff lint/format and diff checks pass. | Retained local acceptance artifacts delivered. This is a suite of scoped synthetic fixtures; the policy comparison is not falsely labeled as the same transfer as every other report. Inventory hashes are not signed assurance. Existing inspected development artifacts were reused; this turn did not perform a fresh checkout/setup or claim remote CI success. Full Python/TS were not repeated for capture-only test/script/workflow/documentation changes; prior full Python is 963 passed and CLI 12 passed. Fresh-environment automation and the requirement-by-requirement M0–M5 acceptance audit remain open. F1–F5 remain open; no live counterparty or production claims. |

| 2026-09-06 | M5 / CP-017 clean-install dependencies and owned local services | A new independent clone exposed ignored/untracked package-lock.json and uv.lock: npm ci and uv sync --frozen could not run. Resolved dependencies in the new checkout, verified npm ci using declared npm 11.9.0 and frozen Python 3.12 installation, and added the lockfiles with public registry sources and no workspace paths. CI now uses frozen installs. Python collection then exposed the missing protobuf runtime required by the committed protoc 6.31.1 TRISA messages; added protobuf>=6.31.1,<7 (resolved 6.33.6), without editing generated files. Fresh locked environment full Python: 962 passed, 91 skipped, 30 warnings before enabling fresh artifacts/services. Full workspace build passes with no cached tasks; TS suite passes (159 SDK, 12 CLI, 62 contract passing/21 artifact-dependent pending), and SDK/contract noEmit checks pass. Added test_pilot_local.py to provision an owned PostgreSQL 18 cluster with a private Unix socket/no TCP, run the EVM acceptance/export command and stop PostgreSQL on success or failure. Its existing-artifact gate passes all 44 tests; independent invalid-artifact testing verifies cleanup and leaves a separate cluster untouched. All manually created test clusters were stopped. Fresh artifact generation from the isolated clone, without a prepared ptau, completed both real profile round trips, one pairing test and 21 EVM tests. Circom binary matches the CI SHA-256 pin. Ruff/workflow/diff checks pass. | The clean-install and service-ownership gaps are addressed. New artifacts remain explicitly unapproved and outside the repository. The next gate is the owned-service acceptance/export run against these fresh artifacts on the committed setup changes, followed by the original requirement-by-requirement M0–M5 audit. No remote CI success, production assurance or F1–F5 completion is claimed. |

| 2026-09-06 | M5 / CP-017 fresh-artifact acceptance, retained offline reproduction and test-cache correction | Advanced the independent clone to the committed locked-dependency/service setup and ran its owned PostgreSQL/EVM command with the newly generated artifacts: 44 passed. Added the existing proof-storage parsing/migration suite to that same runner, then reran the complete owned-service command: 76 passed, 6 warnings, all nine outputs retained and PostgreSQL stopped. The documented offline code reproduced the exact retained report using the fresh clone environment/artifacts after service shutdown; inventory hashes, manifest/file permissions and reviewer-key/person-name exclusion checks pass. Full Python with fresh artifacts: 963 passed, 90 skipped, 30 warnings. A workspace run initially replayed cached results and did not pass artifact inputs explicitly through Turbo; disabled test-result caching and passed the artifact/Python/temp-directory variables. The rerun executed every test task and passed 159 SDK, 12 CLI, 2 content, 2 artifact-metadata and all 83 contract tests with no artifact-dependent pending cases. Build caching remains permitted. Corrected stale storage runbook claims about unfinished authorization and exclusively synthetic proof tests. Added the clean-checkout checkpoint and tested host versions to the operations guide. Ruff/format/diff checks pass. | The documented fresh-checkout local workflow, artifact generation, owned service setup, retained reports/export and independent offline reproduction now have local execution evidence. The wrapper does not infer host cleanliness from a test pass, and keys remain unapproved. This closes the specific clean-setup gap; M0–M5 remains active until the original numbered requirements, invariants, docs/specs and acceptance gates receive their final item-by-item audit. No remote CI, live customer, production or F1–F5 completion claim. |

| 2026-09-06 | Completion audit / CP-014 scoped provider navigation | Created adoption-pilot-acceptance-audit.md to inventory the original 18 work items and cross-cutting gates without treating aggregate passing tests as blanket completion. Confirmed the explicit provider-link requirement was absent from investigation models/API/CLI. Added an optional operator-configured catalogue bounded by tenant, canonical transfer scope and observed source, with HTTPS navigation-only validation, duplicate/per-scope limits and deterministic ordering. Read-only API/queue responses and readable/JSON CLI consumption support optional provider_links; callers cannot supply the catalogue, unknown/foreign scopes or sources cannot reveal links, invalid configuration returns minimized 503, and no link is fetched or opened. Current navigation is separate from retained event evidence and projected states. Thirty focused Python and 13 CLI tests pass, including built-command consumption. Full Python in the locked independent environment: 973 passed, 90 skipped, 30 warnings. The first PostgreSQL check exposed a test fixture with no queue finding; seeded a failed-custody observation to exercise actual queue inclusion. Final owned PostgreSQL/EVM run in that environment: 76 passed, 6 warnings; CLI build and Ruff/format/diff pass. Documented the catalogue and corrected the stale bilateral/provider-link status. | The concrete CP-014 provider-link gap is closed, but the full item and other numbered requirements remain subject to the open audit. CP-017 documentation still needs consolidated compatibility and observability coverage reviewed/completed. No provider URL format is guessed, external navigation performed, remote source authenticity inferred or production/F1–F5 gate closed. M0–M5 remains active. |

| 2026-09-06 | Completion audit / CP-001, CP-002, CP-017 and CP-018 | Added a consolidated compatibility matrix and operator/observability runbook covering actual profile selection, migrations/rollback, scoped reports, readiness limits and publication recovery. Corrected stale discovery inventory, v1-only artifact guidance and commercial capability/counter descriptions. API metadata now uses the installed distribution version instead of hardcoded 0.1.0; health explicitly describes process liveness, and API description no longer implies general compliance certification. API endpoint suite: 21 passed, 5 warnings. Focused parser/chain-reader suite: 61 passed, 1 warning. Reviewed storage migration/row/grammar/rollback coverage against the prior 32 real database cases included in the 76-test PostgreSQL/EVM gate, and cache isolation/concurrency/freshness/invalidation tests. CP-001 and CP-002 are verified locally. Reviewed all five commercial preparation documents and scoped usage service/API plus its real PostgreSQL/JWT acceptance test; CP-018 is verified for local preparation. | CP-017 compatibility/observability gaps are closed; release references, final diff and remaining numbered acceptance reviews stay open. No full service or workspace rerun was needed for this documentation and API-metadata batch; prior locked full Python and owned-service results remain 973 and 76 passed respectively. Commercial proposals are hypothetical, usage is inventory rather than billing, and customer validation/F1–F5 remain open. The full M0–M5 goal remains active. |

| 2026-09-06 | Completion audit / CP-003 discovery and CP-004 canonical records | Reviewed both discovery clients, profile validators, publisher, shared invalid/network vectors, real local TLS tests and API recipient-resolution boundary. Verified exact DID/key purpose/version routing, vetted-IP connections preserving TLS hostname/Host, enterprise exceptions, redirect/rebinding/mixed-DNS rejection and no encryption downgrade. Reviewed transfer/catalog/context/receipt models, canonical encoders and cross-language/schema vectors for exact identities, uint128 quantities, rational valuation, scope and time checks. Current focused suites passed: 161 Python (5 warnings) and 124 TypeScript; three API discovery-failure cases separately passed (5 warnings), asserting neither proving nor either encryption mode executes. Updated the transfer specification to reference implemented current inspection, atomic authorization and v2 projection instead of future work. | CP-003 and CP-004 are verified for their local acceptance scope. TLS metadata is self-declared; schema/digest success is not authenticated issuance, proof validity or permission. Those boundaries and remaining numbered requirements retain their separate open audit rows. This batch changes documentation only and does not require a full service/artifact rerun. F1–F5 and the overall M0–M5 completion gate remain open. |

| 2026-09-06 | Completion audit / CP-012 event persistence and CP-014 investigation lifecycle | Reviewed event/scope models, source grants, immutable encrypted records, tenant transaction/index constraints, pure reconciler, queue pagination, API and readable/JSON CLI. Matched every original lifecycle scenario to direct tests, including process death between record/index insertion, concurrent duplicates, reordering, cross-tenant collisions, missing submission/evidence, timeout/failure, reorg and canonical-block replacement. Added a requirement-to-test evidence map and corrected stale runbook claims that CLI/provider links were unfinished. Focused reconciliation/provider-link checks: 30 passed; CLI investigation checks: 6 passed. Fresh owned PostgreSQL/EVM execution on current source with existing inspected development artifacts: 76 passed, 6 warnings in 131.30 seconds, including built CLI/JWT/HTTP checks. Nine report/export files retained under audit-events-6d8a0ec; successful manifest present and owned PostgreSQL stopped. | CP-012 and CP-014 are verified for local acceptance. This was a current-source service run, not another fresh installation or artifact ceremony. Internal grants and chain observations do not establish provider authenticity or independent finality; CP-013 retains its own trust/semantics review. Other open audit rows, final cross-cutting checks and F1–F5 remain open. Full M0–M5 goal remains active. |
