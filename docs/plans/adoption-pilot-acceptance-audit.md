# Adoption pilot acceptance audit

Audit baseline: `1f357ec`; final source/test baseline: `29870b7`, followed by
the documented formatting and documentation-packaging corrections. Scope is the
original M0–M5 / CP-001–CP-018 [implementation plan](2026-09-05-adoption-pilot-implementation.md).
**Local acceptance complete, September 7, 2026.** Each requirement below has a
source/behavior review and direct evidence; the cross-cutting gates are recorded
below. This is an implementation acceptance record, not an independent security
audit, production certificate or claim of customer adoption.

## Requirement inventory

| Requirement | Evidence to inspect | Audit status / next check |
| --- | --- | --- |
| CP-001 bounded proof parsing and migration | `src/storage/proofs.py`, `src/storage/signals.py`, migrations; real `test_proof_storage.py` suite | **Verified locally:** reviewed bounded canonical field arrays, restricted legacy grammar (no evaluation), dict-row model validation and transactional paged migration. The 32 real database cases in the last 76-test gate cover valid legacy preservation/reconnect, malformed/oversized/executable input rejection, SQL constraints, cancellation and whole-migration rollback. Current focused parser/reader suite: 61 passed. |
| CP-002 reader/deployment cache isolation | `src/chain/reader.py`, chain cache tests, operations cache guide | **Verified locally:** instance-owned bounded caches and copied deployment configuration; monotonic TTL, cached false/zero/absence, coalesced concurrent calls with cancellation shielding, non-cached errors and generation-based invalidation reviewed against tests. In-flight pre-invalidation callers may receive their old result but cannot repopulate the new generation. Current parser/reader suite: 61 passed, including fixture-provider ABI routing; this does not establish remote RPC truth. |
| CP-003 cross-language discovery and egress | Python discovery publisher/client, TS discovery client, shared fixtures, real TLS transport tests | **Verified locally:** Python publisher reproduces the shared 0.4.0 fixture consumed by Node; both validate full DID, exact authority, X25519 key/purpose/fingerprint, suites and versions. Source and real local TLS tests establish vetted-IP connections with original SNI/Host, certificate/hostname checks, no redirect/proxy bypass, blocked mixed/rebound answers, explicit authority/CIDR exceptions and bounded deadlines/bodies. Distinct error classes and three API cases reject before either prover or encryption runs. Current discovery/canonical suites: 161 Python and 124 TS passed; API failure cases: 3 passed. Domain metadata remains self-declared, not recipient authorization. |
| CP-004 canonical transfer/context | `specs/transfer-evidence-v1.md`, protocol transfer/canonical models, TS commitments and fixtures | **Verified locally:** reviewed strict/frozen record models, catalog membership, exact chain/contract assets, uint128 decimal units, rational valuation, participant identities and verifier context tenant/policy/chain/time binding. Shared transfer/context/receipt vectors and schema-export checks match canonical bytes and domain-separated digests across Python/TS; tests reject ambiguous/unknown amounts/assets, inconsistent valuations, changed scope and oversized/unsupported encoding. Current combined suites: 161 Python and 124 TS passed. Corrected stale future-tense service/projection references in the specification; cryptographic binding and authorization remain separate CP-006/007 reviews. |
| CP-005 durable tenant state and issuance/proving | Pilot store, enrollment, registrar, proof preparation/current services and PostgreSQL authorization tests | **Verified locally:** reviewed encrypted immutable records/revisioned heads, full tenant query scope, shared transaction locking, reconnect, actor/request-bound idempotency and atomic proof/receipt/nullifier retention. Closed the missing durable witness connection: preparation reconstructs memberships from retained signed registrar inventories and uses the same enrollment/current-state loader as inspection. New real-proof acceptance covers enrollment → registrar → reconnect → prepared witness → actual pairing, with missing/altered sources, unpublished enrollment, holder/sanctions mismatch, tenant/role, revocation and advanced-head rejection. Current owned PostgreSQL/EVM gate: 77 passed, including failed/cancelled transaction rollback, six-way idempotent races and competing real authorization requests; 39 focused credential/current-root tests passed. See the storage guide for private witness handling and the distinction between preparation and current consumption. |
| CP-006 authenticated credentials | ADRs 0003/0009, credential/issuance circuits, enrollment/root services, adversarial witness tests | **Verified locally:** ADR/model/enrollment/root-trust/composed-circuit/current-expectation review establishes full credential preimage binding, holder-secret knowledge, issuance plus authorized-issuer membership, and independently authenticated root/consent boundaries. Current tests: 63 credential/enrollment/composed checks (real Circom witnesses, including unissued/unauthorized/holder/subject/jurisdiction attacks and two-valid-credential substitution), plus 44 root/current-statement checks passed. The preceding real-pairing gate rejects alternate credential commitments; reviewed real JWT/database enrollment cases passed in the latest 76-test service run. External signatures are not ZK-verified; broader CP-005/007/008 gates remain separate. |
| CP-007 current verifier parity | v2 signal spec, current inspection/authorization services, SDK/CLI, current registry and real EVM fixtures | **Verified locally:** reviewed exact eight-signal v2 statement, independent current reconstruction, durable root/credential/policy selection, signed participant facts, API/SDK/CLI boundaries and publisher-validated contract checkpoints. Aligned Python's explicit zero-nullifier rejection with the current registry and SDK receipt validator; added matching assertions. Current checks: 122 Python tests including real composed/projection/valuation witnesses, 83 contract tests with both development artifact profiles and no pending cases, 7 SDK tests, and 77 PostgreSQL/EVM tests. Coverage includes every signal mutation, real proof for the deployed registry, roots/epochs/revisions, expiry/future time, exact asset/base-unit/tier arithmetic, participants/credential substitution, issuer membership and counterparty facts, revocation, read-only inspection after consumption and duplicate-spend/mirror rejection. V2 exposes no amount tier or SAR flag; legacy pairing is explicitly separate. PostgreSQL owns consumption; contracts trust source-validated publisher checkpoints and cannot independently authenticate private source claims. |
| CP-008 recipient encryption and evidence integrity | Recipient/information/decision authorities, HPKE envelope, pilot cipher, authorization/export tests | **Verified locally:** reviewed independently scoped recipient keys, transfer/context/proof/recipient/time AAD, information approvals, signed receipts/evidence digests, tenant/revision-bound storage AEAD and atomic authorization retention. Real HPKE tests cover wrong recipient/key/AAD, overlapping and retired keys, altered headers/ciphertext and maximum payload; storage/signature tests reject metadata substitution, rebinding, wrong purpose and unresolved compromise. Tightened permissive base64 decoding; eight mutations with identical decoded bytes now reject. Current checks: 93 focused tests; full Python 981 passed/92 skipped; separate owned PostgreSQL/EVM gate 77 passed, including encryption/post-consumption failure rollback, changed-input conflicts, historical retry and recipient decryption after reconnect. Local counterparty source checks signatures and exact envelope digest before acceptance. AEAD/signatures do not prove database completeness, source truth or independently witnessed decision time; those limits and separate historical trust gates remain explicit. |
| CP-009 manifests and doctor | Artifact model/loader and doctor entry point, source inventory, runtime pins and negative artifact tests | **Verified locally:** exact manifest/file pins, no-follow regular-file reads, missing/mixed/layout rejection, historical-v1/current-v2 separation and supported policy-schema checks reviewed. Pinned runtime snapshots, isolated pairing and timeout/cancellation cleanup have direct tests. Current source inventory, compiler and policy-schema digests match the existing development bundle. Added the missing built `doctor` command with bounded, minimized Python handoff and real-bundle acceptance in the local runner. Checks passed: 50 artifact/runtime/real-pairing tests, then 25 artifact tests including new missing-file coverage; 15 CLI tests and final focused doctor checks; 76 PostgreSQL/EVM tests with doctor preflight. All manifests remain development-unapproved. |
| CP-010 policy evaluation | Python policy model/evaluator, signed facts/valuations, policy evaluation API tests | **Verified locally:** reviewed bounded immutable policy/source/rule models, evaluator precedence, independent fact attestations and current loading. Tests cover all four outcomes, missing required facts below thresholds, conflicts, stale/future inputs, unsupported predicates and minimized deterministic reports. Current focused suite: 121 passed, including real Circom valuation/tier checks at minus/equal/plus boundaries and forged-tier rejection. The shared predicate is integer arithmetic; business/legal rules remain Python evaluation with ZK coverage explicitly not established. |
| CP-011 policy comparison and review | Diff/approval/activation services, API and built CLI tests, retained comparison | **Verified locally:** reviewed diff/API/thin CLI, encrypted approval/case retention and separate activation history. Deterministic before/after reports expose matched/edited rule IDs, missing/unsupported inputs, review deltas and counterfactual limits. Current focused checks: 121 Python and 5 policy CLI passed. Reviewed real PostgreSQL/JWT/built-CLI tests in the latest 76-test gate: expected outcomes, predecessor/actor/time retention, reconnect, tenant/role rejection, competing activation/rollback and unchanged read-only record/consumption counts. Corrected stale fact/activation documentation; no live source-document truth or workload forecast implied. |
| CP-012 append-only events | Event model, ingestion/index transactions and process-death tests | **Verified locally:** source/model/storage/API review and real PostgreSQL tests establish encrypted append-only events, tenant/source identity, separate source/ingestion clocks, atomic index writes, concurrent duplicate handling, reordered delivery and tenant collisions. Process-death test kills the child between record/index writes and proves rollback plus retry. Current owned-service gate: 76 passed. See the evidence map below; internal grants do not imply provider signatures. |
| CP-013 provider and bilateral semantics | Fireblocks adapter/intake, signed fixture, bridge/counterparty, local exchange and CLI tests | **Verified locally:** rechecked official Fireblocks signature/migration/transaction/ordering documentation and OpenVASP core specification against the bounded adapter and explicitly local bilateral profile. Reviewed pinned RS512/JWKS, exact-body verification, independently configured workspace/transaction/asset/source bindings, encrypted evidence and JWT relay. Reviewed signed receipt/envelope/information validation, required private person/address fields, exact participants/asset/base units, local dispositions and no fund execution. Current focused checks: 42 passed. Reviewed direct PostgreSQL/JWT/real-proof/built-CLI cases from the latest 77-test gate: byte reconstruction/revalidation, duplicate/reordered delivery, conflict rollback, tenant/role rejection, accept/reject/information-request/pending/timeout, unsupported profile and key overlap/retirement. Corrected stale implementation-status prose. The schema fixture and signatures are synthetic; captured live provider validation, automated JWKS refresh and remote TRP conformance remain external gates. |
| CP-014 readable investigation/queue and provider links | Reconciler, scoped timeline/queue API and CLI | **Verified locally:** reviewed every named lifecycle scenario, deterministic replay, independent states, ages/reasons/owners/actions, scoped provider links, queue continuation and readable/JSON CLI. Current checks: 30 reconciliation/link tests, 6 CLI report tests and 76 PostgreSQL/EVM tests passed; the latter runs built CLI against actual JWT/HTTP/database responses. Read-only record/consumption counts are preserved. See the evidence map below for exact scenario coverage and observation limits. |
| CP-015 historical evidence | Exporter, history statement/status/timing verification, reviewer schema and offline CLI tests | **Verified locally:** reviewed exact revision/hash export, approved reviewer encryption, independent artifact/runtime and six authority layers, real pairing, policy replay, status delegation and RFC 3161 library verification with pinned TSA/certificates/policy/accuracy. Decision, source observation, capture and review times remain separate; operator times are not independent timestamp proof. Reviewed direct adversarial tests in the latest 77-test database gate: missing/swapped records, roots/keys/policy/envelope/signals, missing trust/timestamp, invalid signatures, later compromise, ordinary expiry, unauthorized export and unchanged records/consumption. Current 19 timestamp/CLI tests passed. A fresh process with Python socket connections disabled reproduced the retained encrypted export's exact supported report using its separately configured synthetic trust and review clock. Corrected stale export status. Support is for the recorded local decision; global source truth, beneficiary plaintext validation and production assurance are not inferred. |
| CP-016 observation onboarding/scenarios/metrics | Durable observations/cohort, CLI, bilateral scenarios, combined custody investigation | **Verified locally:** reviewed observation-only records/roles, shared current evaluation, actor-bound retries, immutable v1/v2 compatibility, scoped monotonic timing, cohort denominators, live pagination and API/SDK/CLI consistency checks. Current focused checks: 3 Python and 11 SDK tests passed. Reviewed direct real-proof/PostgreSQL/JWT/built-CLI cases in the latest 77-test gate: four policy outcomes, failed pairing with no policy, rollback/reconnect, old cached results, tenant isolation, observation-as-proof foreign-key rejection and zero consumption. CP-013's reviewed local counterparty dispositions and duplicate/reordered custody join cover the required scenarios. Actual retained outputs independently reconcile six cases/four observations/two missing, three comparable labels/two agreements/one disagreement and exact measured duration sums. Corrected stale onboarding prose. Metrics describe selected synthetic evaluations, not population completeness, unexplained disagreement causes, end-to-end latency or customer value. |
| CP-017 setup, compatibility and operations | Lockfiles, owned local runner, CI, local acceptance and migration guides | **Verified locally:** independently installed locked checkout, full build, fresh development setup for both profiles, 982 Python tests, 83 real-artifact contract tests and 77 owned PostgreSQL/EVM tests passed. Exact retained offline review, report/pin/permission/privacy checks, noEmit and changed-file Ruff/format checks passed. Reviewed compatibility, migrations/rollback, readiness, metrics and recovery guides; corrected stale README/ADR/status claims. Rechecked private repository and public npm 0.3.0 availability. Homepage and docs deployed; fixed workspace content externalization and monorepo-root prebuilt packaging, then verified 16 live pages and three content endpoints. See the final checkpoint below. Local operator trust configuration remains explicit; source/package distribution and production provisioning are separate follow-ons. |
| CP-018 paid-pilot preparation and counters | Historical commercial preparation (now maintained privately), [public usage semantics](../operations/usage-inventory.md), usage service/API and tests | **Verified for local preparation:** all five commercial packet documents reviewed; brief, interview, worksheet, hypothetical pricing and free/paid boundaries are explicit. `usage_inventory` and its PostgreSQL/JWT test establish one scoped metadata snapshot, role/query enforcement, retry/conflict/reconnect and read-only behavior. Corrected stale capability statements and documented excluded publication tables. No outreach, price acceptance or revenue claim; F4 remains open. |

## Cross-cutting closure checks

All original table acceptance requirements and the accompanying M0–M5 prose were
reviewed against the requirement evidence above. Actual witness/pairing checks
remain distinct from policy and source-authority tests.

- **Build and tests:** independent clone at `29870b7`, frozen npm/Python installs,
  full workspace build, fresh local phase-one/phase-two development artifacts and
  both real proof round trips passed. Final Python: 982 passed / 92 optional or
  service skips; separate owned PostgreSQL/EVM: 77 passed. Uncached workspace
  tests: SDK 159, CLI 15, content 2, artifact metadata 2, contracts 83; no pending
  contract cases. TypeScript noEmit passed. All 152 changed Python files pass Ruff
  and format checks; final HPKE formatting preserves the AST and 21 tests pass.
- **End-to-end outputs:** all nine retained reports present, with exact byte-size,
  SHA-256 and artifact pins; private permissions and synthetic key/person-name
  exclusion checked. Four non-consuming observation outcomes, six selected cases,
  four observed/two missing and exact measured-duration totals independently match.
- **Offline review:** after the owned database and EVM stopped, a new process with
  Python sockets disabled reproduced the exact supported historical report under
  separately supplied synthetic trust and the retained review clock.
- **Release and final diff:** current signal/spec/vector/artifact alignment,
  compatibility and migration/recovery guides reviewed; no generated development
  keys added. Changed-document file targets and whole-branch whitespace checks
  pass. README and proof-system/status prose now distinguish legacy/current
  profiles, published packages and source-only local acceptance. Final docs
  packaging explicitly preserves the content package's runtime asset paths.
- **Public accuracy:** GitHub API again reports the main repository private; the
  four public npm packages remain 0.3.0 and the CLI's content dependency remains
  unavailable. Homepage build/lint and mobile/desktop checks pass. Updated docs
  build and all 16 live pages / three content API endpoints pass after repairing
  prebuilt dependency resolution and workspace externalization.

Exact workflow, counts, host versions and local evidence paths are in the
[September 7 clean-checkout checkpoint](../operations/local-pilot-acceptance.md#final-clean-checkout-checkpoint--september-7-2026).
The final docs configuration was validated by a subsequent production build and
live route checks; unrelated proof/service suites were not repeated for that
packaging-only correction. No remote CI success is inferred from local results.
The frozen npm install reports 50 dependency advisories; production dependency
assurance remains open with F5.

F1–F5 are separate follow-on gates. This audit cannot establish live provider
interoperability, customer adoption, willingness to pay, managed distribution,
independent assurance or production readiness from local simulations.

## Event and investigation evidence map

CP-012/014 source review covers `SourceEvent`/`TransferEvent`,
`EventIngestionService`, the tenant transaction and migration 11 index,
`reconcile`, queue pagination, authenticated routes, provider catalogue and
readable/JSON CLI. The index has tenant-scoped identity/sequence uniqueness and
an exact revision-1 encrypted-record foreign key. Supported writers cannot
update retained events. Internal source authentication is an operator grant;
it is not a historical provider signature or independently verified finality.

| Required behavior | Direct acceptance evidence |
| --- | --- |
| Duplicate/out-of-order convergence | Unit test enumerates all lifecycle permutations and duplicate delivery; PostgreSQL test races five identical ingestions, delivers an earlier source sequence later, reconnects and retains the original ingestion time. |
| Invalid authentication / tenant collision | Real signed-JWT event API rejects missing roles, missing tokens and ungranted sources. Service tests reject actor/dimension/time grants and foreign investigation; identical source IDs in another authorized tenant remain separate. |
| Restart during processing | Child process is killed after encrypted insert and before index insertion; transaction-lock acquisition waits for rollback, then investigation is empty and retry inserts one event. Sequence conflicts after insert also roll back both writes. |
| Approval without submission / timeout / failure | Reconciler tests assert independent states and the exact `approval-without-submission`, `counterparty-timeout` and `settlement-failed` findings with ages, owners and next actions. |
| Settled with missing evidence | Explicit finalized-chain observation with missing proof/evidence yields `settled-with-unresolved-evidence`; custody completion alone cannot establish chain finality. |
| Changed canonical block/finality | Reorg and disagreeing-source tests retain conflicts. Same-source replacement tests reverse delivery order and repeat the replacement, preserving the first change age. These check observation semantics, not remote chain truth. |
| Readable report and provider navigation | Built CLI consumes actual loopback HTTP/JWT/PostgreSQL timeline and queue responses; unit/API tests cover optional HTTPS links filtered by tenant, transfer and observed source, with no navigation request executed. |
| Ageing queue / no implicit completeness | Real database traversal covers filtered empty pages, cursor continuation, exact age threshold, reconnect and tenant isolation. Built CLI distinguishes one-page partial output from complete four-page traversal. Pages are separate observations. |
| Read-only replay | Source has no send/consume operation; live-local report/CLI tests compare record and consumption counts before and after reads. A complete six-state report still has no authorization field. |

Provider-byte authentication and mandatory bilateral semantics remain CP-013;
their separate review is not implied by the internal event grants above.
