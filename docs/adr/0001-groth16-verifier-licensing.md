# ADR 0001: Groth16 Verifier Contract Licensing (GPL-3.0)

- **Status:** Accepted (interim), revisit before mainnet deployment
- **Date:** 2026-05-21
- **Deciders:** clearproof maintainers
- **Related:** ROADMAP "Security assurance" track, `REUSE.toml`, ceremony runbook

## Context

`packages/contracts/contracts/Groth16Verifier.sol` is generated by snarkjs
(`zkey export solidityverifier`). snarkjs — including its latest release
(0.7.6, verified against the published npm package on 2026-05-21) — stamps
generated verifiers with a GPL-3.0 SPDX license identifier and
`Copyright 2021 0KIMS association`.

<!-- REUSE-IgnoreStart -->
(The generated file's exact header line is `SPDX-License-Identifier: GPL-3.0`;
quoted here only to document the finding, not to declare this file's license.)
<!-- REUSE-IgnoreEnd -->

Everything else in clearproof is Apache-2.0 (see `REUSE.toml`). This creates a
mixed-licensing situation that regulated VASP legal teams will flag during
vendor review, and it complicates the commercial open-core story: the on-chain
verification contract is the one component every integrator must deploy.

Key facts:

- The verifier is **mechanical output** of a tool from a specific verification
  key. Whether tool output inherits the tool's license is legally contested,
  but the 0KIMS copyright header is an explicit claim we should not ignore.
- Industry precedent for shipping snarkjs GPL-tagged verifiers inside
  permissively licensed projects is widespread (Tornado Cash, Semaphore,
  MACI all did). None are known to have faced enforcement.
- Solidity/EVM deployment is not "distribution" in the classic GPL sense, but
  the source is also published in this repo, which is unambiguous distribution
  — and it is correctly licensed GPL-3.0 here, so that distribution is lawful.
- The current verifier is generated from the **dev trusted setup** and will be
  **regenerated at the production MPC ceremony** regardless. That regeneration
  is the natural decision point.

## Options Considered

### A. Accept GPL-3.0 for the single generated file (status quo, documented)

Keep shipping the snarkjs-generated verifier with its GPL header intact,
disclosed in README + REUSE.toml. Never strip or alter the header.

- ✅ Zero engineering cost; lawful as-is; strong industry precedent
- ✅ On-chain use imposes no obligations on callers of `verifyProof`
  (calling a deployed contract is not linking)
- ❌ VASP counsel will raise it in diligence; some enterprises have blanket
  "no GPL" policies regardless of technical analysis
- ❌ Forks/modifications of the verifier source must stay GPL

### B. Commission/adopt a permissively licensed (Apache-2.0) verifier template

Generate the verifier from a clean-room or permissively licensed template
implementing the same alt_bn128 pairing checks. snarkjs supports custom
templates; the math is standardized (Groth16 over BN254 with EIP-196/197
precompiles).

- ✅ Removes the diligence friction entirely; uniform Apache-2.0 repo
- ✅ Template is reusable across future circuits and ceremonies
- ❌ Real engineering + audit cost; a hand-rolled pairing verifier is
  security-critical code
- ❌ Must be re-validated against snarkjs proofs (parity vectors make this
  testable — see `tests/vectors/`)

### C. Migrate the proving stack to gnark (Apache-2.0) for verifier export

- ✅ Clean licensing end-to-end
- ❌ Full circuit rewrite (Circom → gnark/Go), new audit surface, ecosystem
  churn. Disproportionate for a licensing issue.

### D. Seek upstream clarification from iden3/0KIMS

Ask whether generated verifier output may be used under Apache-2.0, or
whether they would dual-license the template. Cheap, non-committal.

- ✅ Lowest effort; a clear statement resolves diligence questions cheaply
- ❌ No timeline guarantee; answer may be "no"

## Decision

**Interim: Option A, with Options B and D as pre-mainnet work items.**

1. **Now (done):** GPL-3.0 status documented in `README.md` (License section),
   `REUSE.toml` (dedicated annotation + `LICENSES/GPL-3.0-only.txt`), and this
   ADR. The header is preserved verbatim; we never relicense the file.
2. **Next 30 days:** File an upstream issue with iden3/snarkjs asking for
   clarification or dual-licensing of the verifier template (Option D).
   Track: <issue link to be added when filed>.
3. **Before production ceremony (gate):** If diligence feedback from pilot
   VASPs shows GPL is a blocker, fund Option B. The committed parity test
   vector (`tests/vectors/compliance/`) already provides the conformance
   harness a replacement template must pass: identical proofs must verify
   on-chain and off-chain.
4. **At the ceremony:** whichever option is chosen, the verifier is
   regenerated from the production zkey; the licensing decision must be
   recorded here before that regeneration ships.

## Consequences

- Testnet/pilot deployments proceed with the GPL verifier; pilots must be
  told explicitly (pilot limitations document, per ROADMAP).
- The README License section is the single source of truth for integrators.
- This ADR must be revisited (status → "Superseded") when the production
  verifier is generated.
