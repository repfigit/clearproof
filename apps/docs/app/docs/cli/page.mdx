# CLI

`@clearproof/cli` provides local proof generation, verification and a synthetic demo. Published npm version: **0.3.0**, checked September 5, 2026. Development checkout: **0.4.0**.

## Public installation limitation

The published CLI currently depends on `@clearproof/content`, which was unavailable from the public npm registry when checked September 5, 2026. Consequently, `npx @clearproof/cli@0.3.0 --help` fails during dependency installation. A published version does not imply a working public installation.

For CLI evaluation, contributors with repository access can build the local workspaces and run `node packages/cli/dist/index.js --help`. See [quickstart](/docs/quickstart). The proof SDK can be installed independently.

## Proof workflows

With compatible artifacts and a complete input for that proof version:

```bash
clearproof prove --input inputs.json --artifacts ./artifacts --output proof.json
clearproof verify --proof proof.json --artifacts ./artifacts
```

The current verifier reports cryptographic validity and interpreted circuit outputs. Its success exit code is not equivalent to on-chain registry acceptance or authorization to move funds.

## Synthetic demo

```bash
clearproof demo --artifacts ./artifacts
```

The demo uses synthetic, historical inputs and development artifacts. It demonstrates a local proof workflow; it does not establish fresh sanctions state, live credential authenticity, current policy acceptance or safe transfer execution. Artifact presence and compatibility must be checked first. No fixed completion-time claim is made.

## Planned commands

Policy comparison, transfer investigation and historical verification are planned pilot features. They are not claimed as available commands in the published package.

See [quickstart](/docs/quickstart) and [project status](/docs/status).
